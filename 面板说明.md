# Screeps AI v4.0 面板与可视化说明

---

## 一、信息面板（左上角）

每行从上到下：

```
▛ 基地   T:12345   🟢
```

| 字段 | 含义 |
|------|------|
| `▛ 基地` | 面板标题 |
| `T:12345` | 当前游戏 Tick 数（相当于"回合"，每 tick 约 1-3 秒） |
| `🟢 / 🟡 / 🔴` | CPU Bucket 健康度：🟢>5000 正常，🟡2000-5000 节制，🔴<2000 紧急 |

---

```
🌐 GCL2  ███░░░░░░░  32.1%
   ⏱ GCL 45.2Kt 后升级
```

| 字段 | 含义 |
|------|------|
| `🌐 GCL2` | Global Control Level（全局控制等级）。跨房间总计，所有房间的 Controller 升级都会贡献 GCL 进度 |
| `█████░░░░░` | 微型进度条，10 格 = 100% |
| `32.1%` | GCL 当前升级进度 |
| `⏱ GCL 45.2Kt 后升级` | 按最近采集速率估算，还需多少 tick 升到下一级。"Kt" = 千 tick（如 45.2Kt ≈ 45200 tick） |

---

```
🏛 RCL4  ██████░░░░  58%
   ⏱ 预计 8.3Kt 后升级
   ⚠ 4.2Kt 后降级！
```

| 字段 | 含义 |
|------|------|
| `🏛 RCL4` | Room Control Level（房间控制等级）。当前房间的等级，决定可建造的建筑种类和数量 |
| `██████░░░░` | 进度条 |
| `⏱ 预计 8.3Kt 后升级` | 按最近升级速率估算，还需多久升到下一级 |
| `⚠ 4.2Kt 后降级！` | **仅当剩余时间 < 5000 tick 时显示**。Controller 长时间未升级会自动降级，必须尽快安排 Upgrader！ |

---

```
⚡ 能量  ████░░░░░░  680/1300
```

| 字段 | 含义 |
|------|------|
| `⚡ 能量` | 房间当前可用能量 |
| `680` | 当前能量值（available） |
| `1300` | 能量上限（capacityAvailable，由 Spawn + Extensions 总和决定） |
| 颜色 | 🟢>50% 充裕，🟡20-50% 偏低，🔴<20% 危险 |

---

```
🐣孵化中 234t
⏸ 空闲 (使用率72%)
```

| 字段 | 含义 |
|------|------|
| `🐣孵化中 234t` | Spawn 正在生成 Creep，还需 234 tick 完成。显示紫色 `#cc66ff` |
| `⏸ 空闲 (使用率72%)` | Spawn 当前无孵化任务。使用率 = 历史上有孵化的 tick 占比，越高说明 Spawn 越忙 |
| 颜色含义 | 紫色=孵化中，🟡=空闲但能量>40%（异常，应检查为何不生成），灰色=空闲且能量低（正常等待） |

---

```
📈 净能率  +3.2/t
```

| 字段 | 含义 |
|------|------|
| `📈 净能率` | 能量净变化速率 |
| `+3.2/t` | 每 tick 净增 3.2 能量。**正值=积累**（产出>消耗），**负值=亏空**（消耗>产出） |
| 颜色 | 🟢正值（健康），🔴负值（警告） |
| 计算 | 取最近 100 tick 的前后半段平均值之差 ÷ 时间差 |

---

```
👥 8人   采:3  升:2  建:1  修:1  运:1  墙:0
```

| 字段 | 含义 |
|------|------|
| `👥 8人` | 当前房间里你的 Creep 总数 |
| `采` | Harvester 采集者（含传统走动式和定点式合计） |
| `升` | Upgrader 升级者 |
| `建` | Builder 建造者 |
| `修` | Repairer 维修者 |
| `运` | Hauler 运输者 |
| `墙` | WallRepairer 城墙维修 |
| `占` | Claimer 占领者（跨房间续约 Controller） |
| `远` | RemoteHauler 远征运输（跨房间搬能） |

> **提示**：采:0 时会触发警报；升:0 且人数>0 时也会警告。

---

```
📦 3200  |  🏗 3  |  🛡 2  |  💾 8732
```

| 字段 | 含义 |
|------|------|
| `📦 3200` | Container + Storage 中的库存能量总量（缓冲池） |
| `🏗 3` | 当前房间的建造工地数 |
| `🛡 2` | Tower 数量 |
| `💾 8732` | CPU Bucket 余额（上限 10000）。Bucket 每秒恢复，消耗于 CPU 运算 |
| `占:1 远:1` | 远征相关角色统计（仅远程房间启用后出现） |

---

## 二、阶段面板（面板右侧，替代原 Sparkline）

面板右侧显示当前所处的发展阶段及各项目标完成度：

```
┌──────────────────────────┐
│ 🔰 P1/5 基础·初始积累     │
│ ████████░░░░  80%         │
│ ✅ RCL 升到 2         100% │
│ ✅ 采集者就位           1/1 │
│ 🔄 建造 Extensions     3/5 │
│ ✅ 能量正增长        +2.1/t│
└──────────────────────────┘
```

### 五个阶段划分

| 阶段 | 名称 | RCL 范围 | 核心目标 |
|------|------|---------|---------|
| P1 🔰 | 基础·初始积累 | RCL 1 | 建立基础采集线，冲 RCL 2 |
| P2 📦 | 容器·定点经济 | RCL 2-3 | Container+定点采集+搬运流水线，建 Storage |
| P3 🛠 | 建造·设施完善 | RCL 3-4 | 炮塔/城墙/Storage 落成，储能 10K |
| P4 ⚗ | 成长·生产升级 | RCL 4-6 | 炮塔完善/储能 50K/城墙维修/矿物交易 |
| P5 🚀 | 扩张·跨房间 | RCL 6+ | 远征采集、Claimer、Link 网络 |

### 目标状态图标

| 图标 | 含义 |
|------|------|
| ✅ | 已完成 |
| 🔄 | 进行中（有百分比或计数） |
| ⬜ | 未开始 |

每个目标右侧显示具体数值或百分比，当前阶段整体进度由顶部的黄色进度条和百分比表示。

---

## 三、手动操作指南 — 各阶段你需要做什么

虽然 AI 能自动完成大多数工作，但**建筑蓝图必须由你手动放置**（游戏机制限制）。以下按阶段列出所有需要你动手的操作，以及操作方法。

---

### 🔰 P1 — 基础·初始积累（RCL 1）

**目标：尽快冲到 RCL 2，解锁更多建筑。**

| 操作 | 如何操作 | 原因 |
|------|---------|------|
| 🏗️ **放 5 个 Extension** | 切换到**建造模式**（Ctrl+Click 或右上角建筑按钮），选 `Extension`，放在 Spawn 周围 | RCL 1 时的阶段目标之一。Extension 增加能量上限，让 Creep 更大更强 |
| 🏗️ **放 1 个 Container** | 找**一个离 Spawn 最近的 Source**，在它旁边 1 格放 `Container` | 让采集者把矿就近存入容器。即使 RCL 还没到 2，提前放一个能加速（AI 默认 RCL2 才启用容器经济，但手动提前放一个会**自动生效**） |

> **选址提示**：Extension 之间留 1 格空隙方便 Creep 通行。Spawn 周围的空地以 3×3 网格规划会比较整齐。

---

### 📦 P2 — 容器·定点经济（RCL 2–3）

**目标：每个 Source 旁有 Container，StaticHarvester + Hauler 流水线运转。**

| 操作 | 如何操作 | 原因 |
|------|---------|------|
| ✅ **不需要操作** | AI 在 RCL 2 时**自动放置**每个 Source 旁和 Controller 旁的 Container 工地 | `RoomManager._planSourceContainers()` 和 `_planControllerContainer()` 会每 tick 检查，缺了就补工地 |
| 🏗️ **放 1 个 Storage** | 在基地安全位置放 `Storage` | P2 阶段目标之一。Storage 建成后能量开始缓冲，经济大幅稳定 |
| 🏗️ **放 1 个 Tower**（可选） | 在基地中心位置放 `Tower` | 有 Tower 后 AI 会管理它。RCL 3 前 Tower 只能是手动放，但早放早安心 |
| 🛣️ **道路留白** | AI 的 RoadPlanner 会在 Source→Spawn→Controller 之间自动铺路，**你不需要手动铺路** | 但如果某些关键路径 AI 没铺到，可以手动补一两格 |

> **注意**：AI 自动放的 Container 位置是**离 Spawn 最近的可建格**，而不是你心中最完美的点。如果位置不理想（比如地形冲突），可以在 `_planSourceContainers` 函数调低 Spawn 距离权重，或者手动拆了重建。

---

### 🛠 P3 — 设施完善（RCL 3–4）

**目标：防御设施就位，能量缓冲建立。**

| 操作 | 如何操作 | 原因 |
|------|---------|------|
| 🧱 **放城墙（Wall）** | 沿基地外围 1-2 格宽放 `Wall`，把 Spawn/Storage/Extensions 围起来 | **AI 不会自动放墙**，这是你的主要防御工作。墙围好后 WallRepairer 会自动维护 |
| 🛡️ **放 Rampart** | 在**关键建筑**（Spawn/Tower/Storage）顶上放 `Rampart` | Rampart 保护建筑不被直接攻击，AI 的 WallRepairer 也会维护 |
| 🗼 **放 Tower**（如果还没放） | 在**覆盖范围最广**的位置放 `Tower` | Tower 越早放越好。位置选能覆盖 Source→Spawn→Controller 走廊的高地 |
| 🏗️ **放 Extension（续建）** | RCL3 解锁更多 Extension 上限，继续补充到满 | 更多 Extension = 能量上限更高 = Creep 更大更强更快 |

> **城墙布局参考**：不需要围满全地图。一个 7×7 到 9×9 的方形围栏把核心建筑包住就够了，留 1-2 格宽的口子给 Creep 进出（Rampart 可以替代出口处的缺口）。

---

### ⚗ P4 — 成长·生产升级（RCL 4–6）

**目标：储能 50K，Link 网络就绪，矿物交易运转。**

| 操作 | 如何操作 | 原因 |
|------|---------|------|
| 🗼 **补满 Tower** | RCL 4-5 解锁更多 Tower 上限。放 2-3 个 Tower 覆盖基地各方向 | 阶段目标 "炮塔完善" 要求 ≥ 3 个 Tower |
| 🧱 **加固城墙** | 在薄弱方向加厚墙，或在关键隘口放 Rampart | P4 敌人变强，墙和 Rampart 需要足够的厚度 |
| ⛏️ **放 Extractor**（可选） | 在**矿物节点（Mineral）**上放 `Extractor` | 虽然 AI **不会自动挖矿**（只从市场买），但放了 Extractor 后你可以自己安排采集。矿物自采可以减少 Credits 消耗 |
| 🏗️ **放 Link**（RCL5+） | 在每个 **Source 旁边 2 格内**放一个 Link，在 **Controller 旁边 3 格内**放一个 Link | RCL5 解锁 Link 后，AI 的 `LinkManager` 会自动把能量从 Source Link 送到 Controller Link。这大幅节省搬运工 |
| 🔌 **放 Terminal**（RCL6） | 系统自动解锁，在任意空地放 `Terminal` | `MarketManager` 在 RCL4 就就绪了，但需要 Terminal 建好才能交易 |
| 🏗️ **放 Labs**（RCL6） | AI **自动放置** Lab 工地（在 Controller 3 格范围内搜索空地） | `LabManager._planLabs()` 每 tick 自动检查，缺就补 |

> **Link 摆放技巧**：Source 旁的 Link 尽量贴近 Source（≤2 格），Controller 旁的 Link 尽量贴近 Controller（≤3 格）。两个 Link 可以在同一房间任意距离——Link 传输不消耗 tick、不受距离影响。

---

### 🚀 P5 — 扩张·跨房间（RCL 6+）

**目标：跨房间采集能量，Lab 强化 Upgrader。**

| 操作 | 如何操作 | 原因 |
|------|---------|------|
| 🚩 **放远程 Flag** | 在**当前房间**任意位置放一个 Flag，命名为 `remote_目标房间名`，例如 `remote_W1N1` | `RemoteMonitor` 每 tick 扫描 Flag，自动注册远程房间并开始派 Claimer 和 RemoteHauler |
| 📦 **在远程 Source 旁放 Container** | 切换到目标房间视野（点击地图），在每个 Source 旁边 1 格放 `Container` | **AI 不会自动在远程房间放 Container**。没有 Container，RemoteHauler 就无法取能 |
| 🔬 **补满 Labs** | RCL6 解锁 3 个 Lab。AI 自动放 3 个，但还可以手动多放形成反应链 | Lab 强化需要 3 个 Lab 组成链，AI 的 LabManager 会自动处理购买→送药→强化全流程 |
| 🏗️ **放 Link（继续补）** | RCL6 解锁更多 Link 上限。可以在 Storage 旁边也放一个 Link 做中转 | 进阶布局：Storage Link + Controller Link + Source Link 形成三节点网 |
| 🛡️ **强化远程房间防御**（可选） | 在远程房间手动放 Tower 和 Wall | 远征的价值取决于远程房间的安全。不设防的远程房间可能被敌人打掉 Container |

> **Flag 命名示例：**
> - `remote_W1N1` → 注册 W1N1 为远程房间
> - `remote_E2S3` → 注册 E2S3 为远程房间
> - 删除 Flag → AI 自动清除该房间的远程记录，停止派 Creep

---

### 📌 总结速查表

| 阶段 | 你必须手动做的事 | AI 自动做的事 |
|------|----------------|-------------|
| **P1** | 放 5 个 Extension | 孵化 Creep、采集、升级、建造 |
| **P2** | （可选）放 Storage、Tower | 自动放 Container、定点采集、搬运 |
| **P3** | **放 Wall 和 Rampart**、补 Tower | 修墙、修路、防御、规划道路 |
| **P4** | 补满 Tower、放 Link（RCL5）、Terminal（RCL6） | 市场交易、Link 输电、储能 |
| **P5** | **放 `remote_` Flag**、远程放 Container | 远征续约、跨房间搬能、Lab 强化 |

> 💡 **一条原则**：只要涉及**建筑摆放位置**的事，都需要你手动放蓝图。AI 只管**自动放 Container 和 Lab**（因为位置有明确的就近规则）。墙、炮塔、Storage、Extension 的位置太依赖你的战略判断，交给 AI 放可能会放在奇怪的地方。

---

## 四、能量流动画

地图上的动态流线：

| 颜色 | 样式 | 路径 | 含义 |
|------|------|------|------|
| 🟡 `#ffaa00` | 虚线 + 往复粒子 | Source → Container | 采集者正在从 Source 挖矿存入旁边 Container |
| 🟢 `#66ff66` | 虚线 + 往复粒子 | Container → Spawn | 搬运工正在将 Container 中的能量送往 Spawn |
| 🔵 `#00ccff` | 点线 + 往复粒子 | Storage/Spawn → Controller | 升级者从 Storage/Spawn 取能量升级 Controller |

每条线上有往复脉动的小光点，只在有实际能量流转时显示。Container 上方还有数字显示当前能量。

---

## 五、进度条（地图上）

| 位置 | 颜色 | 内容 |
|------|------|------|
| Controller 上方 | 青色 `#00ffff` | RCL 等级 + 进度% + ETA |
| 每个工地 | 黄色 `#ffff00` | 建造完成百分比 |
| Spawn 上方 | 紫色 `#cc66ff` | 孵化中的角色名 + 剩余 tick |

---

## 六、Source 标记

每个 Source 上方：

| 标记 | 含义 |
|------|------|
| `⛏️2` | 传统走动式采集者 ×2（无 Container 或 RCL 太低） |
| `📦1` | 定点采集者 ×1（旁边有 Container，静态采集） |
| 虚线 | Source 到 Container 的连线（表示采集流关系） |

---

## 七、Creep 头顶标签

每个 Creep 头顶显示当前状态：

| 标签 | 状态 | 谁会有 |
|------|------|--------|
| **采集** | HARVESTING | 所有角色取能量时 |
| **存放** | WORKING | Harvester 存能量时 |
| **升级** | UPGRADING | Upgrader / 其他角色 fallback |
| **建造** | BUILDING | Builder |
| **维修** | REPAIRING | Repairer / WallRepairer |
| **运输** | HAULING | Hauler 运送时 |

寿命警告：

| TTL | 显示 | 含义 |
|-----|------|------|
| > 300 | `采集` | 正常 |
| 100–299 | `采集·243` | 附加剩余 tick 数 |
| < 100 | `💀87` | 即将死亡，需补人 |

---

## 八、警报系统

面板下方脉冲条，只在异常时显示：

| 警报 | 触发条件 | 等级 |
|------|---------|------|
| `💀 无 Creep！` | 所有 Creep 死亡 | 🔴 致命 |
| `⚠ 无采集者` | 没有任何 harvester/staticHarvester | 🔴 致命 |
| `⚔ N敌人` | 房间内有敌对 Creep | 🔴 危险 |
| `🔴 能量XXX` | 能量 < 15% | 🔴 危险 |
| `⏬ RCL降级` | Controller 剩余 < 3000 tick 不升级 | 🔴 危险 |
| `⚠ 无升级者` | 有 Creep 但没有 upgrader | 🟡 警告 |
| `⏸ Spawn空闲` | 能量 > 50% 但 Spawn 没在孵化 | 🟡 警告 |
| `💾 Bucket低` | CPU Bucket < 2000 | 🟡 警告 |
| `💾 Bucket极低!` | CPU Bucket < 500 | 🔴 危险 |

---

## 九、Flag 可视化开关

在房间内放置对应名称的 Flag 即可切换可视化级别（无需改代码）：

| Flag 名称 | 效果 |
|-----------|------|
| （无 flag） | **默认模式** — 面板 + 趋势图 + 流线 + 进度条 + Source 标记 |
| `viz_off` | **关闭全部可视化** — 纯游戏画面，CPU 零开销 |
| `viz_min` | **精简模式** — 仅左上角面板 + 底部警报条 |
| `viz_debug` | **调试模式** — 完整可视化 + 面板标题显示"调试"字样 |

### 使用示例

```
场景 1: 正常玩游戏
  → 不放 flag，默认完整可视化

场景 2: 截图分享 / 录视频
  → 放一个名为 viz_min 的 flag，只保留面板和警报

场景 3: CPU 紧张 / 大量 Creep
  → 放 viz_off 关闭所有可视化

场景 4: 调试 AI 行为
  → 放 viz_debug 确认面板数据是否正常
```

Flag 实时生效：放下即切换，拔掉即恢复默认。无需保存代码或重新运行。

---

## 十、CPU 降级与可视化的关系

可视化使用 `RoomVisual` API，**不消耗 CPU**。但可视化代码本身的逻辑（字符串拼接、循环等）会消耗微量 CPU。

当 CPUGovernor 处于 `critical` 模式（Bucket < 2000）时，所有可视化自动跳过。

| CPU 层级 | Bucket | 可视化 | 说明 |
|----------|--------|--------|------|
| 🟢 normal | > 5000 | 全开 | |
| 🟡 moderate | 2000–5000 | 全开 | 日志频率降低 |
| 🔴 critical | < 2000 | **全部关闭** | 保 CPU 给核心逻辑 |

---

## 十一、控制台可调配置

以下参数位于 `main.js` 顶部的 `CONFIG` 对象中，**在 Screeps 编辑器中直接改数字即可生效**，无需重写逻辑。

### 11.1 日志级别

```js
LOG_LEVEL: 1
```

| 值 | 控制台输出内容 | 适用场景 |
|----|--------------|---------|
| `0` | 全部 — 每个 Creep 的每次行动、状态切换、Source 分配… | 调试 AI 行为、排查 bug |
| `1` | **一般** — 生成事件、警报、能量预警、每 tick 一行摘要 | 正常游戏（推荐） |
| `2` | 仅警告 — 攻击、敌人入侵、能量严重不足、Spawn 受损 | 挂机托管，不想看刷屏 |
| `3` | 仅错误 — 崩溃、Spawn 丢失、未知角色 | 极度精简 |

### 11.2 种群控制

```js
POPULATION: {
  MIN_HARVESTERS: 1,    MAX_HARVESTERS: 6,
  MIN_UPGRADERS:  1,    MAX_UPGRADERS:  4,
  MIN_BUILDERS:   0,    MAX_BUILDERS:   3,
  MIN_REPAIRERS:  0,    MAX_REPAIRERS:  2,
  MIN_HAULERS:    1,    MAX_HAULERS:    3,
  MIN_WALL_REPAIRERS: 0,MAX_WALL_REPAIRERS: 1,
  MIN_CLAIMERS:   0,    MAX_CLAIMERS:   1,
  MIN_REMOTE_HAULERS: 0,MAX_REMOTE_HAULERS: 2,
  MAX_STATICHARVESTERS: 4,
}
```

| 参数 | 默认 | 说明 |
|------|------|------|
| `MIN_HARVESTERS` | 1 | 最少保证几个走动式采集者（RCL1 的唯一采集来源） |
| `MAX_HARVESTERS` | 6 | 传统采集者上限。RCL2+ 应减少，改由 staticHarvester 替代 |
| `MIN_UPGRADERS` | 1 | 最少升级者。**想加速 RCL 升到 2 或 3** |
| `MAX_UPGRADERS` | 4 | 升级者上限。能量充裕时可调高追 RCL |
| `MIN_BUILDERS` | 0 | 无工地时不生建造者 |
| `MAX_BUILDERS` | 3 | 工地多时可调高加速建造 |
| `MIN_HAULERS` | 1 | RCL2+ 保证至少 1 个搬运工（容器→基地运输线） |
| `MAX_HAULERS` | 3 | 搬运工上限。距离远或 Container 多时可调高 |
| `MIN/MAX_REPAIRERS` | 0/2 | 道路破损时自动生成。可在修路完成后调回 0 |
| `MIN/MAX_WALL_REPAIRERS` | 0/1 | RCL3+ 自动生成。想快速堆墙 HP 可手动调高 MAX |
| `MIN/MAX_CLAIMERS` | 0/1 | P5 远征——跨房间续约 Controller |
| `MIN/MAX_REMOTE_HAULERS` | 0/2 | P5 跨房间能量搬运 |
| `MAX_STATICHARVESTERS` | 4 | 定点采集者上限（按 Source 数自动限制） |

### 11.3 维修阈值

```js
REPAIR: {
  ROAD_THRESHOLD:      0.5,
  WALL_THRESHOLD:      0.0001,
  STRUCTURE_THRESHOLD: 0.5,
  WALL_MAX_RATIO:      0.001,
}
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `ROAD_THRESHOLD` | 0.5 | 道路 HP 降到 50% 以下时开始修 |
| `STRUCTURE_THRESHOLD` | 0.5 | Container 等建筑 50% 以下修理 |
| `WALL_THRESHOLD` | 0.0001 | 墙/Rampart 修到 0.01% HP 为止（RCL 低时省能量） |
| `WALL_MAX_RATIO` | 0.001 | WallRepairer 只处理 HP < 0.1% 的墙 |

> **调参建议**：RCL4 有 Storage 之后，把 `WALL_THRESHOLD` 改为 `0.001`（0.1%），`WALL_MAX_RATIO` 改为 `0.01`（1%），让墙真正有防御力。

### 11.4 能量阈值

```js
ENERGY: {
  TOWER_MIN_ENERGY: 500,
}
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `TOWER_MIN_ENERGY` | 500 | Tower 能量低于此值不会用于维修（保留弹药）。下调可让 Tower 多修路，上调可保留更多弹药防御 |

### 11.5 路径 & CPU

```js
PATH: {
  REUSE_TICKS: 25,
},
CPU: {
  MODERATE_BUCKET:  5000,
  CRITICAL_BUCKET:  2000,
},
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `REUSE_TICKS` | 25 | 路径缓存 tick 数。调高可省 CPU 但 Creep 可能走老路；调低路径更准但更耗 CPU |
| `MODERATE_BUCKET` | 5000 | Bucket 低于此值进入节制模式（跳过高开销日志） |
| `CRITICAL_BUCKET` | 2000 | Bucket 低于此值进入紧急模式（关可视化、关 Tower 维修） |

### 11.6 邮件通知

```js
NOTIFY: {
  ENABLED:       true,    // 总开关
  ON_HOSTILES:   true,    // 敌人入侵时通知
  ON_ALL_DEAD:   true,    // 所有 Creep 死亡时通知
  ON_STARTUP:    true,    // 脚本启动时发送现状报告
  ON_LOW_ENERGY: true,    // 能量持续 < 15% 时通知
  INTERVAL:      1440,    // 同类通知最小间隔 (分钟, 1440 = 24小时)。单位是分钟不是 tick！原值 172800 超出 API 上限被拒绝
},
```

| 参数 | 默认 | 说明 |
|------|------|------|
| `ENABLED` | true | 总开关。关掉后所有通知都不发 |
| `ON_HOSTILES` | true | 敌人入侵时发送邮件。单次：敌人来发一次，全部撤离后重置 |
| `ON_ALL_DEAD` | true | 所有 Creep 死亡时发送邮件。有新 Creep 生成后自动重置 |
| `ON_STARTUP` | true | 每次粘贴新脚本后发送一封现状报告 |
| `ON_LOW_ENERGY` | true | 能量跌到 15% 以下时发送（需持续 LOW_ENERGY_TICKS 后才发，避开孵化瞬时低谷）。恢复后重置 |
| `INTERVAL` | 1440 | 同类通知最小间隔。⚠️ 单位是**分钟**不是 tick！Game.notify 的 groupInterval 上限 1440（24h），原值 172800 被 API 拒绝导致分组失效 |
| `LOW_ENERGY_TICKS` | 300 | 能量需连续低于 15% 这么久才发信，避免 Spawn 孵化瞬时低谷误报 |

### 测试通知

在 Screeps 控制台（Console）中输入以下命令即可发送测试邮件：

```js
Game.notify("测试通知 - 如果收到这封邮件说明通知系统正常")
```

### 排查通知不生效

1. **控制台确认** — 如果发送成功，控制台会显示 `📧 [通知] 已发送邮件: xxx`
2. **邮箱验证** — Screeps 官网账户必须验证邮箱才能收到通知
3. **私服无效** — `Game.notify()` 只在官方服务器（shard0/1/2/3）生效

### 11.7 容器采集

```js
CONTAINER: {
  ENABLE_AT_RCL:       2,
  WITHDRAW_THRESHOLD:  200,
  PICKUP_THRESHOLD:    50,
}
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `ENABLE_AT_RCL` | 2 | 几级开始启用定点采集+搬运模式。如果手动提前建了 Container 可改为 1 |
| `WITHDRAW_THRESHOLD` | 200 | 从 Container 取能的最低存量。调高可保留 Container 缓冲，调低可更快搬空 |
| `PICKUP_THRESHOLD` | 50 | 地上掉落能量超过此值才去捡 |

### 11.8 远程/远征

```js
REMOTE: {
  MAX_REMOTE_ROOMS: 2,
  RESERVE_TICKS:    3000,
  CLAIM_FLAG_PREFIX: "remote_",
  MIN_STORAGE_ENERGY: 30000,
  HAUL_AMOUNT_MIN:  500,
  CLAIMER_RENEW_AT: 500,
}
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `MAX_REMOTE_ROOMS` | 2 | 最多管理几个远程房间 |
| `CLAIM_FLAG_PREFIX` | "remote\_" | Flag 名前缀，房间中放置 `remote_W1N1` 格式 Flag 即可注册远程房间 |
| `MIN_STORAGE_ENERGY` | 30000 | Storage 能量低于此值时不出征 |
| `HAUL_AMOUNT_MIN` | 500 | 远程容器能量不足此值时不派车搬运 |

### 11.9 Link 网络

```js
LINKS: {
  ENABLED_AT_RCL:  5,
  SOURCE_RANGE:    2,
  CONTROLLER_RANGE: 3,
  SEND_THRESHOLD:  400,
  RECEIVE_MAX:     600,
}
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `ENABLED_AT_RCL` | 5 | RCL5 解锁 Link 后启用自动输电 |
| `SOURCE_RANGE` | 2 | Source 2 格内的 Link 视为输入端 |
| `CONTROLLER_RANGE` | 3 | Controller 3 格内的 Link 视为输出端 |
| `SEND_THRESHOLD` | 400 | 输入 Link 能量 ≥ 400 时才发送 |
| `RECEIVE_MAX` | 600 | 输出 Link 能量超过此值时不再接收（防溢出） |

### 11.10 市场交易

```js
MARKET: {
  ENABLED_AT_RCL:  4,
  ENERGY_BUY_MIN:  0.01,
  ENERGY_BUY_MAX:  0.05,
  ENERGY_BUY_AMOUNT: 2000,
  ENERGY_BUY_THRESHOLD: 10000,
  MINERAL_SELL_MIN: 0.05,
  MINERAL_SELL_AMOUNT: 500,
  STORAGE_SELL_THRESHOLD: 0.8,
  TRADE_INTERVAL: 100,
  TERMINAL_ENERGY_RESERVE: 5000,
}
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `ENABLED_AT_RCL` | 4 | RCL4 解锁 Terminal 后启用自动交易 |
| `ENERGY_BUY_MIN/MAX` | 0.01-0.05 | 买入能量的价格区间 |
| `ENERGY_BUY_AMOUNT` | 2000 | 单次买入量 |
| `ENERGY_BUY_THRESHOLD` | 10000 | Storage 能量低于此值开始买入 |
| `MINERAL_SELL_MIN` | 0.05 | 卖出矿物的最低单价 |
| `MINERAL_SELL_AMOUNT` | 500 | 单次卖出量 |
| `STORAGE_SELL_THRESHOLD` | 0.8 | Storage 填充率 > 80% 时才卖出矿物 |
| `TRADE_INTERVAL` | 100 | 两次扫盘间隔 tick（防止每 tick 调用 Game.market.getAllOrders） |
| `TERMINAL_ENERGY_RESERVE` | 5000 | Terminal 保留的最小能量（用于支付交易运费） |

### 11.11 实验室强化

```js
LABS: {
  ENABLED_AT_RCL: 6,
  MIN_LABS: 3,
  BOOST_COMPOUNDS: ["GH", "UH", "LH"],
  COMPOUND_BUY_MAX: 0.5,
  COMPOUND_BUY_AMOUNT: 300,
  BOOST_MIN_ENERGY: 20000,
  LAB_RANGE: 3,
}
```

| 参数 | 默认 | 含义 |
|------|------|------|
| `ENABLED_AT_RCL` | 6 | RCL6 解锁 Lab 后启用自动强化 |
| `MIN_LABS` | 3 | 至少建 3 个 Lab 才启动（保留反应链扩展空间） |
| `BOOST_COMPOUNDS` | ["GH","UH","LH"] | 优先购买的反应物（GH=GH2O 升级提速，UH=UH 矿工提速） |
| `COMPOUND_BUY_MAX` | 0.5 | 化合物最高买入单价 |
| `COMPOUND_BUY_AMOUNT` | 300 | 每次买入量 |
| `BOOST_MIN_ENERGY` | 20000 | Storage 能量低于此值时不消耗化合物强化 |
| `LAB_RANGE` | 3 | 在 Controller 多少格内搜索空地放置 Lab |

---

> **改动方式**：在 Screeps 编辑器中打开 `main.js`，找到顶部 `CONFIG` 对象，改数字 → 保存 → 下一个 tick 立即生效。不用重启、不用清内存。
