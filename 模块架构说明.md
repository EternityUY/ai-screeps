# Screeps AI v3.0 模块架构与运行流程说明

> 说明：本文档描述 `main.js` 中各个"模块"（逻辑单元）的职责、启动阶段、运行流程和相互依赖关系。所有模块均在同一文件内、按固定顺序在每 tick 串行执行。

---

## 目录

1. [整体架构概览](#1-整体架构概览)
2. [核心支撑模块](#2-核心支撑模块)
   - 2.1 CPUGovernor — CPU 预算降级控制
   - 2.2 RoomCache — 房间缓存层
   - 2.3 Helpers — 通用工具
   - 2.4 Migration — 版本迁移
   - 2.5 Logger — 日志系统
   - 2.6 Notifier — 邮件通知
3. [角色模块](#3-角色模块)
   - 3.1 Harvester — 采集者（走动式）
   - 3.2 StaticHarvester — 定点采集者
   - 3.3 Hauler — 运输者
   - 3.4 Upgrader — 升级者
   - 3.5 Builder — 建造者
   - 3.6 Repairer — 维修者
   - 3.7 WallRepairer — 城墙维修者
4. [管理层模块](#4-管理层模块)
   - 4.1 RoomManager — 房间管理器
   - 4.2 TowerManager — 炮塔管理器
   - 4.3 SpawnManager — 孵化管理器
   - 4.4 StatsTracker — 统计追踪器
5. [可视化模块](#5-可视化模块)
   - 5.1 Visuals — 信息面板与可视化
6. [主循环时序](#6-主循环时序)
7. [模块启动阶段总览](#7-模块启动阶段总览)

---

## 1. 整体架构概览

本 AI 是一个单房间、基于角色（Role-based）的经济脚本。每 tick 执行一个固定的串行流水线：

```
┌─────────────────────────────────────────────────────────┐
│  Migration.run()         版本迁移（仅脚本更新后执行）     │
├─────────────────────────────────────────────────────────┤
│  Helpers.cleanMemory()   清理已死亡 Creep 的内存          │
├─────────────────────────────────────────────────────────┤
│  RoomCache.refresh()     缓存房间内所有建筑/单位数据      │
├─────────────────────────────────────────────────────────┤
│  countCreepsByRole()     统计每种角色的存活数量           │
├─────────────────────────────────────────────────────────┤
│  RoomManager.run()       房间状态监测 + 预警              │
├─────────────────────────────────────────────────────────┤
│  TowerManager.run()      炮塔逻辑（攻击→治疗→维修）      │
├─────────────────────────────────────────────────────────┤
│  _runAllCreeps()         遍历所有 Creep 执行角色行为      │
├─────────────────────────────────────────────────────────┤
│  SpawnManager.run()      高优先级序列决定下一个孵化目标   │
├───────────────────┬─────────────────────────────────────┤
│  StatsTracker       │ Visuals.run()                     │
│  .record()          │ 信息面板/可视化绘制                │
│  记录历史数据       │                                    │
└───────────────────┴─────────────────────────────────────┘
```

**核心设计原则：**
- 通过 `RoomCache` 实现每 tick 仅一次 `room.find()`，多个模块共享同一份缓存数据。
- `CPUGovernor` 三级降级策略，在 Bucket 紧张时自动关闭非关键功能。
- 通过 `countCreepsByRole()` 在 tick 早期一次性统计角色数量，后续所有决策复用该统计结果。

---

## 2. 核心支撑模块

### 2.1 CPUGovernor — CPU 预算降级控制

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L108-125 |
| 启动阶段 | 主循环中每 tick 被各模块主动查询 |
| 是否需要房间 | 否 |

**运行逻辑：**

```
Bucket > 5000?  → normal   层级: 全功能运行
Bucket 2000-5000? → moderate 层级: 跳过 INFO 级日志
Bucket < 2000?  → critical  层级: 关可视化+关 Tower 维修
```

- `shouldSkipInfoLog()` — moderate 及以上返回 true。
- `shouldSkipVisual()` — critical 层级返回 true。
- `shouldSkipTowerRepair()` — critical 层级返回 true。

其他模块在关键决策前查询 CPUGovernor 的层级，而非自行判断 bucket 值。

---

### 2.2 RoomCache — 房间缓存层

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L198-261 |
| 启动阶段 | 第二阶段（主循环第 2 步） |
| 是否需要房间 | 是 → 必须有 `homeRoom` |

**运行流程：**

```
RoomCache.refresh(room)
  ├── room.find(FIND_STRUCTURES)          ← 唯一一次 find()
  ├── room.find(FIND_SOURCES)
  ├── room.find(FIND_CONSTRUCTION_SITES)
  ├── room.find(FIND_DROPPED_RESOURCES)
  └── room.find(FIND_HOSTILE_CREEPS)
       │
       └── 遍历分类将结果写入 cache 对象:
            spawns, extensions, towers, containers, storage,
            roads, walls, ramparts, sources, sites, dropped,
            hostiles, myCreeps, controller, energyAvailable,
            energyCapacityAvailable
```

其他模块通过 `RoomCache.get(roomName)` 读取数据，无需再调用 `room.find()`。这是性能优化的核心——将 O(n) 次 find() 降为 O(1) 次。

**注意：** 当前仅缓存 `homeRoom`，预留了按 `roomName` 索引的扩展接口。

---

### 2.3 Helpers — 通用工具

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L420-468+ |
| 涉及角色 | 全体 |

**关键方法：**

| 方法 | 功能 |
|------|------|
| `getHomeRoom()` | 获取主房间名（根据第一个 Spawn 所在房间） |
| `cleanMemory()` | 遍历 `Memory.creeps`，删除 `Game.creeps` 中已死亡的条目 |
| `countCreepsByRole(roomName)` | 遍历 RoomCache 中的 `myCreeps`，按角色名统计数量 |
| `genName(prefix)` | 生成唯一名称 `{prefix}_{Game.time}`（如 `采集_12345`） |
| `isTowerRampart(pos)` | 判断某个位置是否被 Rampart 保护（用于 Tower 安全维修） |

---

### 2.4 Migration — 版本迁移

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1757-1829 |
| 启动阶段 | 第一执行（主循环第一步） |
| 执行时机 | 仅在脚本版本号变化后执行一次 |

**迁移流程：**

```
Migration.run()
  ├── 读取 Memory.scriptVersion
  ├── 与当前版本号比较
  ├── 若不一致:
  │   ├── v1→v2: working 布尔值 → state 枚举
  │   ├── v2→v3: 清理旧字段
  │   ├── 发送启动通知（现状报告）
  │   └── 更新 Memory.scriptVersion
  └── 若一致: 无操作
```

---

### 2.5 Logger — 日志系统

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L129-192 |
| 级别 | 0=全部 / 1=一般 / 2=警告 / 3=错误 |

**日志格式：** `[模块名] [表情符号] 消息`

示例：
```
[主循环]   🎮 首次运行
[升级者]   ⬆️ 升级:Controller +0.2 (12%)
[采集者]   🚚 存入 Spawn
[房间管理] 🟡 能量偏低: 680/1300 (52%)
[Tower]    ⚔️ 攻击入侵者 attacker (60TP)
```

各模块通过 `Logger.info()`、`Logger.warn()`、`Logger.error()` 输出控制台日志。

---

### 2.6 Notifier — 邮件通知

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L473-521 |
| 触发时机 | 主循环中被 RoomManager/其他模块调用 |

发送条件：
- 总开关 `CONFIG.NOTIFY.ENABLED` 必须为 true
- 同类通知有最小间隔控制 `CONFIG.NOTIFY.INTERVAL`（默认 172800 tick ≈ 48 小时）
- 通过 `Memory._ntf[key]` 记录上次发送时间实现去重

---

## 3. 角色模块

每个角色是一个数据对象，包含 `run(creep)` 方法。所有角色在 `_runAllCreeps()` 中被遍历调度，通过 `creep.memory.role` 匹配对应模块。

> **状态机：** 所有角色使用统一的状态枚举（`"harvesting"` ←→ `"working"/"upgrading"/"building"/"repairing"/"hauling"`），而非旧的 `working` 布尔值。

### 3.1 Harvester — 采集者（走动式）

| 属性 | 值 |
|------|-----|
| 角色名 | `harvester` |
| 启动阶段 | **第一阶段** — RCL1 即开始工作 |
| 启用条件 | 始终启用（`MIN_HARVESTERS: 1`） |
| 孵化顺序 | 优先级最高 |

**运行流程：**

```
Harvester.run(creep)
  ├── state === "harvesting"
  │   ├── 分配目标 Source（负载均衡: 选 Creep 数最少的 Source）
  │   ├── 移动到 Source
  │   └── 采集能量 (harvest)
  │
  └── state === "working"
      ├── 就近选择存储目标 (Spawn > Extension > Tower)
      ├── 移动到目标
      └── 存入能量 (transfer)
      └── 如果旁边有 Container → 改为存入 Container（本版本新增）
```

**身体部件模式：** `[WORK,WORK,CARRY,MOVE]` 重复，每组 350 能量，根据 `energyCapacityAvailable` 自动缩放。

**Source 分配策略：** 遍历所有 Source，对每个 Source 统计已分配且状态为 harvesting 的 Creep 数，选最少的那个。Creep 会将分配的 Source ID 保存在 `memory.sourceId` 中，避免每 tick 重新分配。

---

### 3.2 StaticHarvester — 定点采集者

| 属性 | 值 |
|------|-----|
| 角色名 | `staticHarvester` |
| 启动阶段 | **第二阶段** — RCL ≥ 2 时启用 |
| 启用条件 | RCL ≥ 2 且每个 Source 已有 Container |
| 孵化顺序 | 第 2 优先级 |

**运行逻辑：** 与 Harvester 共用 `run` 方法，但行为上有两个关键区别：

1. **身体部件偏重 WORK：** `[WORK,WORK,WORK,CARRY,MOVE]` 每组 450 能量，采集效率更高。
2. **存储逻辑自动切换：** 当检测到 Source 旁边有 Container 时，采集完成后直接存入 Container，不返回基地 → _定点不动，成为静态矿工_。
3. 同时在 Source 和 Container 之间绘制流线。

**设计意图：** 将"走动式采集"和"定点采集"分离。StaticHarvester 负责高效产出，Harvester 在 RCL1 过渡期负责确保能量供应。RCL2+ 后 Harvester 逐步减少，依赖 StaticHarvester + Hauler 的组合。

---

### 3.3 Hauler — 运输者

| 属性 | 值 |
|------|-----|
| 角色名 | `hauler` |
| 启动阶段 | **第二阶段** — RCL ≥ 2 时启用 |
| 启用条件 | RCL ≥ 2 且 `MIN_HAULERS: 1` |
| 孵化顺序 | 第 3 优先级（首次）、第 7 优先级（补充） |

**运行流程：**

```
Hauler.run(creep)
  ├── state === "hauling"
  │   ├── 检查是否有掉落能量 > PICKUP_THRESHOLD (50)
  │   ├── 否: 从 Container 取能（需存量 > WITHDRAW_THRESHOLD (200)）
  │   ├── 否: 从 Storage 取能
  │   └── 移动到目标取能
  │
  └── state === "working"
      ├── 优先存入 Spawn/Extension
      ├── 其次存入 Tower（若能量低于 TOWER_MIN_ENERGY (500)）
      └── 最后存入 Storage
```

**设计意图：** 在 StaticHarvester 将能量存入 Container 后，Hauler 负责将 Container 的能量搬运到基地。分离了"采集"和"运输"两个职责。

**身体部件模式：** `[CARRY,CARRY,MOVE]` 重复，每组 150 能量。高 Carry 容量适合运输。

---

### 3.4 Upgrader — 升级者

| 属性 | 值 |
|------|-----|
| 角色名 | `upgrader` |
| 启动阶段 | **第一阶段** — RCL1 即开始工作 |
| 启用条件 | 始终启用（`MIN_UPGRADERS: 1`） |
| 孵化顺序 | 第 4 优先级（首次）、第 9 优先级（补充，上限 4） |
| 兜底作用 | 其他角色无任务时也会回退到升级 |

**运行流程：**

```
Upgrader.run(creep)
  ├── state === "harvesting"
  │   ├── 从 Container 取能量（优先）
  │   ├── 没有 Container: 从 Storage 取
  │   ├── 没有 Storage: 从 Spawn/Extensions 取
  │   └── 移动到目标取能
  │
  └── state === "upgrading"
      └── 移动到 Controller 并 upgradeController
```

**兜底行为：** Builder、Repairer、WallRepairer 在没有目标工作时，都会回退到 `"upgrading"` 状态，成为临时升级者。这确保能量始终被有效利用，不会闲置。

**身体部件模式：** `[WORK,WORK,CARRY,MOVE]` 重复，每组 350 能量。

---

### 3.5 Builder — 建造者

| 属性 | 值 |
|------|-----|
| 角色名 | `builder` |
| 启动阶段 | **第一阶段** — RCL1 即开始工作 |
| 启用条件 | 有 ConstructionSite 且 `MAX_BUILDERS: 3` |
| 孵化顺序 | 第 5 优先级 |

**运行流程：**

```
Builder.run(creep)
  ├── state === "harvesting"
  │   └── 从容器/Spawn/Storage 取能
  │
  └── state === "building"
      ├── 按优先级选择工地:
      │   EXTENSION > TOWER > STORAGE > SPAWN > CONTAINER > ROAD > WALL > RAMPART
      ├── 移动到工地
      └── 建造 (build)
      │
      └── 无工地 → state 切换为 "upgrading"（回退）
```

**健康检查：** `hasConstructionSites()` — 房间内有工地时才投入 Builder 配额，无工地时 Builder 数量降为 0。

---

### 3.6 Repairer — 维修者

| 属性 | 值 |
|------|-----|
| 角色名 | `repairer` |
| 启动阶段 | **第一阶段** — RCL1 即开始维修 |
| 启用条件 | 有损坏道路/容器时 |
| 孵化顺序 | 第 6 优先级 |
| 数量上限 | `MAX_REPAIRERS: 2` |

**运行流程：**

```
Repairer.run(creep)
  ├── state === "harvesting"
  │   └── 从容器/Spawn/Storage 取能
  │
  └── state === "repairing"
      ├── 筛选可维修目标:
      │   filter: h/hmax < REPAIR.ROAD_THRESHOLD (0.5) — 道路
      │       或  h/hmax < REPAIR.STRUCTURE_THRESHOLD (0.5) — 容器
      ├── 按受损程度排序（最严重的优先）
      ├── 移动到目标
      └── 维修 (repair)
      │
      └── 无损坏目标 → state 切换为 "upgrading"（回退）
```

**设计意图：** 专注维修道路和容器，城墙维修交由专门的 WallRepairer。

---

### 3.7 WallRepairer — 城墙维修者

| 属性 | 值 |
|------|-----|
| 角色名 | `wallRepairer` |
| 启动阶段 | **第三阶段** — RCL ≥ 3 时启用 |
| 启用条件 | RCL ≥ 3 且有墙/rampart 低于阈值 |
| 孵化顺序 | 第 8 优先级 |
| 数量上限 | `MAX_WALL_REPAIRERS: 1` |

**运行流程：**

```
WallRepairer.run(creep)
  ├── state === "harvesting"
  │   ├── 从 Storage 取能（优先）
  │   └── 没有 Storage: 从 Container 取能
  │
  └── state === "repairing"
      ├── 筛选: h/hmax < WALL_MAX_RATIO (0.001 = 0.1% HP) 的墙
      ├── 按 HP 从低到高排序
      ├── 移动到目标
      └── 维修 (repair)
      │
      └── 无目标 → state 切换为 "upgrading"（回退）
```

**阈值说明：**
- `WALL_THRESHOLD` (0.0001) — 这是 Tower 维修时的墙终值（0.01% HP）
- `WALL_MAX_RATIO` (0.001) — 这是 WallRepairer 筛选标准，只修 HP < 0.1% 的墙

**设计意图：** 城墙需要海量能量来维修（RCL1-3 不需要），因此延迟到 RCL3+ 有 Storage 后才启动。低 RCL 时通过 Tower 维持最低血量即可。

---

## 4. 管理层模块

### 4.1 RoomManager — 房间管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1111-1187 |
| 启动阶段 | 主循环第 4 步（紧接角色统计之后） |
| 依赖 | RoomCache、CPUGovernor |

**运行流程：**

```
RoomManager.run(counts)
  ├── 1. 能量预警
  │   ├── energyAvailable / energyCapacityAvailable < 0.15 → 🔴 红色预警（每 100 tick 一次日志）
  │   └── < 0.30 → 🟡 黄色预警
  │
  ├── 2. RCL 进度报告（每 100 tick）
  │   ├── 显示当前 RCL、进度%、ETA
  │   └── 降级剩余 < 5000 tick → ⚠️ 警告
  │
  ├── 3. 敌对检测
  │   ├── 检测 RoomCache 中 hostiles 列表
  │   ├── 发现敌人 → 激活安全模式（controller.sign 可用且未激活时）
  │   ├── 更新 Memory._hadHostiles 追踪敌情变化
  │   └── 触发邮件通知（按 NOTIFY.ON_HOSTILES 配置）
  │
  ├── 4. Spawn 健康检查（每 100 tick）
  │   └── 检测 Spawn.hits 是否 < hitsMax
  │
  └── 5. 调试可视化（LOG_LEVEL <= 0 时）
      └── 在房间左上角显示调试信息覆盖层
```

---

### 4.2 TowerManager — 炮塔管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1042-1107 |
| 启动阶段 | 主循环第 4 步（紧跟 RoomManager） |
| 依赖 | RoomCache、CPUGovernor |
| 优先级 | 攻击 > 治疗 > 维修 |

**运行流程：**

```
TowerManager.run()
  ├── 遍历所有 Tower (RoomCache.get().towers)
  │
  ├── 1. 攻击 (ATTACK)
  │   ├── 检查 hostiles 列表
  │   ├── 目标评分: HEAL * 3 + (ATTACK + RANGED_ATTACK) * 2
  │   ├── 选择最高分目标
  │   ├── 攻击
  │   └── 记录入侵日志（首次发现时）
  │
  ├── 2. 治疗 (HEAL) — 仅当无敌人时执行
  │   ├── 筛选 10 格内 HP% < 0.8 的己方 Creep
  │   └── 治疗 HP% 最低的 Creep
  │
  └── 3. 维修 (REPAIR) — 仅当能量 > TOWER_MIN_ENERGY (500) 且 CPU 非 critical
      ├── 筛选 h/hmax < REPAIR.ROAD_THRESHOLD (0.5) 的道路
      ├── 筛选 h/hmax < REPAIR.CONTAINER_THRESHOLD (0.5) 的容器
      ├── 按受损程度排序
      └── 维修最严重的目标
```

**CPU 降级联动：** critical 模式时跳过维修步骤，保留攻击和治疗。

---

### 4.3 SpawnManager — 孵化管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L891-1039 |
| 启动阶段 | 主循环第 6 步（所有 Creep 跑完之后） |
| 依赖 | RoomCache、BodyBuilder、counts |

**优先级队列（数字越小越优先）：**

```
优先级 | 角色              | 触发条件
───────┼───────────────────┼────────────────────────────────────
  1    | harvester         | 数量 < MIN_HARVESTERS (1)
       |                   | 或 (harvester+staticHarvester) < 总人口 40%
  2    | staticHarvester   | RCL ≥ 2 且数量 < Source 数
  3    | hauler            | RCL ≥ 2 且数量 < MIN_HAULERS (1)
  4    | upgrader          | 数量 < MIN_UPGRADERS (1)
  5    | builder           | 有工地且数量 < MAX_BUILDERS (3)
  6    | repairer          | 有损坏道路且数量 < MAX_REPAIRERS (2)
  7    | hauler            | 数量 < MAX_HAULERS (3) [补充]
  8    | wallRepairer      | RCL ≥ 3 且数量 < MAX_WALL_REPAIRERS (1)
  9    | upgrader          | 数量 < MAX_UPGRADERS (4) [补充]
 10    | harvester         | 兜底补充
```

**孵化流程：**

```
SpawnManager.run(counts)
  ├── 按优先级逐项检查条件
  ├── 找到第一个需要孵化的角色
  │
  └── _trySpawn(roleName)
      ├── BodyBuilder.build(roleName)
      │   └── 根据 energyCapacityAvailable 动态缩放模式部件
      ├── 总能量成本 > energyAvailable?
      │   └── 是 → 回退到基础模板 [WORK, CARRY, MOVE] (200 能量)
      ├── spawnCreep(name, body, {memory})
      │   ├── 命名: Helpers.genName(角色前缀)
      │   └── 初始化 memory: { role, state: "harvesting", homeRoom }
      └── 成功 → 记录日志; 失败 → 跳过
```

**身体部件模板（BODY_PATTERNS）：**

| 角色 | 单组模式 | 每组能量 |
|------|---------|---------|
| harvester | `[WORK,WORK,CARRY,MOVE]` | 350 |
| staticHarvester | `[WORK,WORK,WORK,CARRY,MOVE]` | 450 |
| upgrader | `[WORK,WORK,CARRY,MOVE]` | 350 |
| builder | `[WORK,WORK,CARRY,MOVE]` | 350 |
| repairer | `[WORK,CARRY,MOVE]` | 200 |
| hauler | `[CARRY,CARRY,MOVE]` | 150 |
| wallRepairer | `[WORK,CARRY,MOVE]` | 200 |

所有角色的身体部件均使用 **模式重复法**（BodyBuilder 自动重复每组配置，直到能量上限不足或达到最大部件数），避免手动计算比例。

---

### 4.4 StatsTracker — 统计追踪器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1194-1278 |
| 启动阶段 | 主循环第 7 步（SpawnManager 之后） |
| 依赖 | RoomCache、Memory.stats |

**运行流程与存储的数据：**

```
StatsTracker.record(homeRoom)
  ├── 1. rclLog (最近 50 tick)
  │   └── 记录 controller.progress / progressTotal
  │
  ├── 2. gclLog (最近 100 tick)
  │   └── 记录 Game.gcl.progress / progressTotal
  │
  ├── 3. energyLog (最近 100 tick)
  │   └── 记录 room.energyAvailable
  │
  └── 4. spawnActivity
      └── 记录 spawn 是否在工作
```

**分析功能：**
- `energyRate()` — 最近 100 tick 前后半段能量的平均差值 ÷ 时间差 → 净能率
- `rclETA()` / `gclETA()` — 基于最近 10 个样本的线性回归 → 升级 ETA
- `spawnUptime()` — Spawn 忙碌 tick 占比 → 使用率

---

## 5. 可视化模块

### 5.1 Visuals — 信息面板与可视化

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1310-1701 |
| 启动阶段 | 主循环第 9 步（最后一步） |
| 依赖 | RoomCache、StatsTracker、CPUGovernor |
| CPU 开销 | RoomVisual 本身零 CPU，但逻辑有微量开销 |
| 开关方法 | 房间内放置 Flag: `viz_off` / `viz_min` / `viz_debug` |

**绘制内容（按绘制顺序）：**

```
Visuals.run()
  ├── 1. 能量流动画
  │   ├── 🟡 Source → Container（采集流）
  │   ├── 🟢 Container → Spawn（运输流）
  │   └── 🔵 Storage/Spawn → Controller（升级流）
  │
  ├── 2. 信息面板（左上角，矩形背景）
  │   ├── 标题行: ▛ 基地 T:{tick} 🟢/🟡/🔴
  │   ├── GCL 进度: 🌐 GCL{n} ██░░ 32.1%  ⏱ ETA
  │   ├── RCL 进度: 🏛 RCL{n} ██░░ 58%    ⏱ ETA ⚠降级
  │   ├── 能量: ⚡ 能量 ██░░ 680/1300
  │   ├── 孵化: 🐣孵化中 / ⏸空闲
  │   ├── 净能率: 📈 +3.2/t
  │   ├── 角色统计: 👥 8人 采:3 升:2 建:1 修:1 运:1 墙:0
  │   └── 资源统计: 📦 3200 🏗 3 🛡 2 💾 8732
  │
  ├── 3. 阶段面板（面板右侧）
  │   ├── 当前阶段标识 (P1/2/3/4) + 名称
  │   ├── 整体进度条 + 完成百分比
  │   └── 各项目标: ✅已完成 / 🔄进行中 / ⬜未开始
  │
  ├── 4. 进度条（地图上）
  │   ├── Controller: 青色 RCL{n} {进度%} ETA
  │   ├── 工地: 黄色 {完成度%}
  │   └── Spawn: 紫色 {角色名} {剩余 tick}
  │
  ├── 5. Source 标记
  │   ├── ⛏️N → 传统采集者数量
  │   └── 📦N → 定点采集者数量（有 Container）
  │
  ├── 6. Creep 头顶状态标签
  │   ├── 采集 / 存放 / 升级 / 建造 / 维修 / 运输
  │   └── TTL < 300 时附加剩余 tick，< 100 时显示 💀
  │
  └── 7. 底部警报条（脉冲动画）
      └── 按严重程度: 💀全灭 > ⚔敌人 > 🔴能量低 > ... > 🟡Spawn空闲
```

**可视化级别控制（通过 Flag，无需改代码）：**

| Flag 名称 | 效果 |
|-----------|------|
| （无 flag） | 默认完整可视化 |
| `viz_off` | 关闭全部可视化（CPU 零开销） |
| `viz_min` | 仅左上方面板 + 底部警报条 |
| `viz_debug` | 完整可视化 + 额外调试标注 |

---

## 6. 主循环时序

完整的主循环执行顺序（`module.exports.loop`）：

```
Step  ┆ 模块              ┆ 功能                               ┆ 阶段/条件
──────┼───────────────────┼────────────────────────────────────┼──────────────────
  1   ┆ Migration.run()   ┆ 版本迁移（仅脚本更新后的第一个 tick）┆ 无条件优先
  2   ┆ cleanMemory()     ┆ 清理死亡 Creep 的内存               ┆ 每 tick
  3   ┆ RoomCache.refresh()┆ 缓存房间数据（一次 find 供全 tick） ┆ 每 tick
  4   ┆ countCreepsByRole()┆ 统计存活角色数量                     ┆ 每 tick
  5a  ┆ RoomManager.run() ┆ 房间状态监测、预警、安全模式          ┆ 每 tick
  5b  ┆ TowerManager.run()┆ 炮塔: 攻击→治疗→维修                ┆ 每 tick
  6   ┆ _runAllCreeps()   ┆ 遍历所有 Creep 执行角色逻辑          ┆ 每 tick
  7   ┆ SpawnManager.run()┆ 按优先级孵化新 Creep                 ┆ 每 tick
  8   ┆ StatsTracker.rec()┆ 统计数据记录                          ┆ 每 tick
  9   ┆ _report()         ┆ 控制台单行摘要输出                    ┆ 每 tick
 10   ┆ Visuals.run()     ┆ 可视化面板/流线 (critical 时跳过)    ┆ 每 tick
```

**异常处理：** 整个主循环包裹在 try/catch 中。连续崩溃 5 tick 后触发 `Game.notify()` 邮件告警。

---

## 7. 模块启动阶段总览

以下按 5 个发展阶段划分各模块/角色的启动时机和目标体系。游戏内面板右侧的 **阶段面板** 会实时显示当前处于哪个阶段及各目标完成进度。

---

### 🔰 P1 — 基础·初始积累（RCL 1）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 2 | ✅ 已实现 | Controller 进度达到 100% |
| 采集者就位 | ✅ 已实现 | 至少 1 个 Harvester 存活工作 |
| 建造 Extensions | ✅ 已实现 | 建造至少 5 个 Extension 扩展容量 |
| 能量正增长 | ✅ 已实现 | 净能率 > 0，经济自持 |

**实际运行中的角色：**

| 模块 | 说明 |
|------|------|
| **Harvester** | 走动式采集者，直接挖矿→送回 Spawn，RCL1 唯一能量来源 |
| **Upgrader** | 升级 Controller 冲 RCL2，至少 1 个常驻 |
| **Builder** | 有工地时自动建造，优先级 EX > TOWER > STORAGE |
| **Repairer** | 道路/容器破损时自动维修 |
| **TowerManager** | 炮塔防御（有 Tower 就开始） |
| **RoomManager** | 房间监测预警从一开始就运行 |

> 阶段目标：尽快积累 300 能量升级 RCL2，进入容器经济。

---

### 📦 P2 — 容器·定点经济（RCL 2–3）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 3 | ✅ 已实现 | Controller 进度达到 100% |
| Container 覆盖 | ✅ 已实现 | 每个 Source 旁至少放好 1 个 Container |
| 定点采集就位 | ✅ 已实现 | 每个 Source 配 1 个 StaticHarvester |
| 搬运工就位 | ✅ 已实现 | 至少 1 个 Hauler 在运输线上 |
| 建造 Storage | ✅ 已实现 | Storage 建成，能量开始缓冲 |

**新启用模块：**

| 新启用模块 | 说明 |
|-----------|------|
| **StaticHarvester** | 在每个 Source 旁放 Container 后，定点采集者取代走动式采集者 |
| **Hauler** | 专职搬运工，连接 Container→基地 |
| 🔄 **Harvester → 减少** | 逐步被 StaticHarvester 替代，从主力变为补充/兜底 |

> 阶段特征：采集→Container→Hauler→Spawn/Storage 的完整流水线形成。Harvester 不再需要走回基地，效率大幅提升。

---

### 🛠 P3 — 建造·设施完善（RCL 3–4）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 4 | ✅ 已实现 | Controller 进度达到 100% |
| 建造炮塔 | ✅ 已实现 | 至少 1 个 Tower 覆盖基地 |
| 城墙/路障 | ✅ 已实现 | 建造城墙和路障围护关键设施 |
| Storage 落成 | ✅ 已实现 | Storage 建成 |
| 储能达标 10K | ✅ 已实现 | Storage 积累 10,000 能量缓冲 |

**新启用模块：**

| 新启用模块 | 说明 |
|-----------|------|
| **WallRepairer** | 城墙/路障维修（RCL3+ 有 Storage 后启动） |
| **Tower 维修** | Tower 在能量充裕时修补墙/道路 |

> 阶段特征：RCL3 解锁 Tower/Wall/Rampart，从纯经济建设转向设施完善 + 初步防御。

---

### ⚗ P4 — 成长·生产升级（RCL 4–6）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 6 | ✅ 已实现 | Controller 进度达到 100% |
| 炮塔完善 | ✅ 已实现 | 建造至少 3 个 Tower 形成防御火力网 |
| 储能达标 50K | ✅ 已实现 | Storage 积累 50,000 能量供扩张使用 |
| 城墙维修者 | ✅ 已实现 | WallRepairer 自动维持城墙耐久 |
| 矿物/交易系统 | ⬜ 未完成 | 矿物开采 + 终端 + Market 交易（预留） |

**持续运作的角色：**

| 模块 | 说明 |
|-----------|------|
| **WallRepairer** | 城墙/路障持续维修（该阶段成为常驻角色） |
| **所有 P1-P3 角色** | 持续运作，产能爬坡 |
| **矿物/交易** | ⬜ 代码骨架预留（CONFIG 占位），功能未实现 |

> 阶段特征：从生存建设转向产能爬坡，储能攀升至 50K 为跨房间扩张做储备。矿物和交易系统等待后续开发。

---

### 🚀 P5 — 扩张·跨房间（RCL 6+）

| 目标 | 状态 | 说明 |
|------|------|------|
| 远征采集 | ⬜ 未完成 | RemoteHarvester 派遣到相邻房间采集（BODY_PATTERNS 已预留模板） |
| Claimer 就绪 | ⬜ 未完成 | Claimer 占领新房间的 Controller（BODY_PATTERNS 已预留模板） |
| Link 网络 | ⬜ 未完成 | Link 建筑间输电优化物流（CONFIG 已预留参数） |
| 多房间运营 | ✅ 部分实现 | 同时管理 ≥2 个房间（RoomCache 已预留 roomName 索引） |

---

## 附录 A：数据依赖关系图

```
┌─────────────────────────────────────────────────────────────────────┐
│                          migration.run()                             │
│                        (Memory.scriptVersion)                        │
└─────────────────────────────────────────────────────────────────────┘
                                    │
┌─────────────────────────────────────────────────────────────────────┐
│                        cleanMemory()                                │
│                   (Memory.creeps ⟂ Game.creeps)                     │
└─────────────────────────────────────────────────────────────────────┘
                                    │
┌─────────────────────────────────────────────────────────────────────┐
│  RoomCache.refresh(room)  ────  room.find() ×5                       │
│       │                                                              │
│       ├── spawns / extensions / towers / containers / storage        │
│       ├── roads / walls / ramparts                                   │
│       ├── sources / sites / dropped / hostiles / myCreeps            │
│       └── controller / energyAvailable / capacityAvailable           │
└───────────┬─────────────────────────────────────────────────────────┘
            │
            ▼
┌─────────────────────┐   ┌──────────────────┐   ┌───────────────────┐
│ countCreepsByRole() │   │ RoomManager.run() │   │ TowerManager.run()│
│   (myCreeps list)   │   │ (cache + counts)  │   │ (cache + hostiles)│
└─────────┬───────────┘   └──────────────────┘   └───────────────────┘
          │                                                                   
          ▼                                                                   
┌──────────────────────────────────────────────────────────────────────────────┐
│ _runAllCreeps()                                                              │
│   Game.creeps → modules[role].run(creep)                                     │
│     ├── Harvester   (harvester + staticHarvester)                            │
│     ├── Upgrader    (upgrader)                                               │
│     ├── Builder     (builder)                                                │
│     ├── Repairer    (repairer)                                               │
│     ├── Hauler      (hauler)                                                 │
│     └── WallRepairer(wallRepairer)                                           │
│   全部通过 RoomCache.get() 读取数据，避免重复 find()                          │
└──────────────────────────────────────────────────────────────────────────────┘
            │                                                                   
            ▼                                                                   
┌─────────────────────┐   ┌──────────────────┐   ┌───────────────────┐
│ SpawnManager.run()  │   │ StatsTracker     │   │ Visuals.run()     │
│ (counts + cache)    │   │ .record(cache)   │   │ (cache + stats)   │
│ 孵化优先级队列      │   │ 写入 Memory.stats│   │ RoomVisual 绘制   │
└─────────────────────┘   └──────────────────┘   └───────────────────┘
```
