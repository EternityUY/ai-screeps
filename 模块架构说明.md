# Screeps AI v4.0 模块架构与运行流程说明

> 说明：本文档描述 `main.js` 中各个"模块"（逻辑单元）的职责、启动阶段、运行流程和相互依赖关系。所有模块均在同一文件内、按固定顺序在每 tick 串行执行。

---

## 目录

1. [整体架构概览](#1-整体架构概览)
2. [核心支撑模块](#2-核心支撑模块)
   - 2.1 CPUGovernor — CPU 预算降级控制
   - 2.2 RoomCache — 房间缓存层
   - 2.3 Helpers — 通用工具
   - 2.4 Migration — 版本迁移
   - 2.5 Logger — 日志系统
   - 2.6 Notifier — 邮件通知
   - 2.7 BodyBuilder — 动态身体部件生成
   - 2.8 Emergency — 采集者灭绝自救
3. [角色模块](#3-角色模块)
   - 3.1 Harvester — 采集者（走动式）
   - 3.2 StaticHarvester — 定点采集者
   - 3.3 Hauler — 运输者
   - 3.4 Upgrader — 升级者
   - 3.5 Builder — 建造者
   - 3.6 Repairer — 维修者
   - 3.7 WallRepairer — 城墙维修者
   - 3.8 Claimer — 远征占领者
   - 3.9 RemoteHauler — 远征运输者
4. [管理层模块](#4-管理层模块)
   - 4.1 RoomManager — 房间管理器
   - 4.2 TowerManager — 炮塔管理器
   - 4.3 SpawnManager — 孵化管理器（优先级队列 + 预生成替换）
   - 4.4 LinkManager — Link 网络管理器
   - 4.5 MarketManager — 市场交易管理器
   - 4.6 LabManager — 实验室强化管理器
   - 4.7 RemoteMonitor — 远程 Flag 探测
   - 4.8 RemoteManager — 远征管理器
   - 4.9 RoadPlanner — 道路自动规划
   - 4.10 StatsTracker — 统计追踪器
5. [可视化模块](#5-可视化模块)
   - 5.1 VisConfig — 可视化 Flag 开关
   - 5.2 Visuals — 信息面板与可视化
6. [主循环时序](#6-主循环时序)
7. [模块启动阶段总览](#7-模块启动阶段总览)

---

## 1. 整体架构概览

本 AI 是一个单房间、基于角色（Role-based）的经济脚本。每 tick 执行一个固定的串行流水线：

```
┌──────────────────────────────────────────────────────────────┐
│  Migration.run()             版本迁移（仅脚本升级时执行一次） │
├──────────────────────────────────────────────────────────────┤
│  Helpers.cleanMemory()       清理已死亡 Creep 的内存          │
├──────────────────────────────────────────────────────────────┤
│  RoomCache.refresh()         缓存房间内所有建筑/单位数据      │
├──────────────────────────────────────────────────────────────┤
│  RemoteMonitor.run()         扫描 remote_* Flag 注册远程房间  │
├──────────────────────────────────────────────────────────────┤
│  countCreepsByRole()         统计每种角色的存活数量           │
├──────────────────────────────────────────────────────────────┤
│  RoomManager.run()           房间状态监测 + 预警 + 自动规划   │
│    ├── Container 自动放置（Controller + Source 旁）           │
│    └── 调试可视化（LOG_LEVEL=0 时）                           │
├──────────────────────────────────────────────────────────────┤
│  RemoteManager.run()         远征数据刷新 + 状态日志           │
├──────────────────────────────────────────────────────────────┤
│  RoadPlanner.run()           道路网络自动规划（每 200t）      │
├──────────────────────────────────────────────────────────────┤
│  TowerManager.run()          炮塔逻辑（攻击→治疗→维修）      │
├──────────────────────────────────────────────────────────────┤
│  LinkManager.run()           RCL5+ Source→Controller 自动输电 │
├──────────────────────────────────────────────────────────────┤
│  MarketManager.run()         RCL4+ Terminal 能量买入/矿物卖出 │
├──────────────────────────────────────────────────────────────┤
│  LabManager.run()            RCL6+ Lab 规划 + 化合物购买 + 强化│
├──────────────────────────────────────────────────────────────┤
│  _runAllCreeps()             遍历所有 Creep 执行角色行为       │
│    ├── 死锁检测（无采集者+无法复活 → Emergency 模式）         │
│    └── 各角色 run()          按 role 分发                     │
├──────────────────────────────────────────────────────────────┤
│  SpawnManager.run()          优先级队列 → 预生成 → 孵化      │
├──────────────────────────────────────────────────────────────┤
│  StatsTracker.record()       记录历史数据（energy/RCL/GCL）   │
├──────────────────────────────────────────────────────────────┤
│  _report()                   控制台单行摘要输出               │
├──────────────────────────────────────────────────────────────┤
│  Visuals.run()               信息面板 + 阶段面板 + 可视化     │
│    (CPU critical 时跳过)                                      │
└──────────────────────────────────────────────────────────────┘
```

**核心设计原则：**
- 通过 `RoomCache` 实现每 tick 仅一次 `room.find()`，多个模块共享同一份缓存数据。
- `CPUGovernor` 三级降级策略，在 Bucket 紧张时自动关闭非关键功能。
- 通过 `countCreepsByRole()` 在 tick 早期一次性统计角色数量，后续所有决策复用该统计结果。

---

## 2. 核心支撑模块

### 2.1 CPUGovernor — CPU 预算降级控制

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L162-179 |
| 启动阶段 | 主循环中每 tick 被各模块主动查询 |
| 是否需要房间 | 否 |

**运行逻辑：**

```
Bucket > 5000?  → normal   层级: 全功能运行
Bucket 2000-5000? → moderate 层级: 仅停用非关键日志
Bucket < 2000?  → critical  层级: 关可视化+关 Tower 维修+关日志
```

- `shouldSkipInfoLog()` — **critical** 层级返回 true（moderate 级别不跳过日志）。
- `shouldSkipVisual()` — moderate 及以上返回 true（即仅 normal 显示可视化）。
- `shouldSkipTowerUtil()` — critical 层级返回 true（关掉 Tower 非攻击行为）。

其他模块在关键决策前查询 CPUGovernor 的层级，而非自行判断 bucket 值。

---

### 2.2 RoomCache — 房间缓存层

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L252-317 |
| 启动阶段 | 第二阶段（主循环第 2 步） |
| 是否需要房间 | 是 → 必须有 `homeRoom` |

**运行流程：**

```
RoomCache.refresh(room)
  ├── room.find(FIND_STRUCTURES)          ← 一次遍历拆分所有结构类型
  ├── room.find(FIND_SOURCES)
  ├── room.find(FIND_MY_CONSTRUCTION_SITES)
  ├── room.find(FIND_DROPPED_RESOURCES)
  ├── room.find(FIND_HOSTILE_CREEPS)
  └── room.find(FIND_MY_CREEPS)
       │
       └── 遍历分类将结果写入 cache 对象:
            spawns, extensions, towers, containers, storage, terminal,
            links, labs, roads, walls, ramparts, sources, sites,
            dropped, hostiles, myCreeps, controller, energyAvailable,
            energyCapacityAvailable, rcl
```

其他模块通过 `RoomCache.get(roomName)` 读取数据，无需再调用 `room.find()`。这是性能优化的核心——将 O(n) 次 find() 降为 O(1) 次。

**注意：** 按 `roomName` 索引，支持多房间扩展。`refresh()` 可为任意房间调用，远程房间在 `RemoteMonitor.refreshCache()` 中复用同一机制。

---

### 2.3 Helpers — 通用工具

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L456-561 |
| 涉及角色 | 全体 |

**关键方法：**

| 方法 | 功能 |
|------|------|
| `getHomeRoom()` | 获取主房间名（遍历 Game.spawns 取第一个的 room.name） |
| `getHomeSpawn()` | 从 RoomCache 获取第一个 Spawn |
| `cleanMemory()` | 遍历 `Memory.creeps`，删除 `Game.creeps` 中已死亡的条目 |
| `countCreepsByRole()` | **从 Game.creeps 全局遍历**按角色统计。不接收 roomName 参数，避免因跨房间 Creep 离开 homeRoom 导致计数偏低 |
| `findToRepair(roomName)` | 从缓存过滤道路/容器/城墙/壁垒中需维修的结构，按损坏程度排序 |
| `getRCL(roomName)` | 从 RoomCache 获取 RCL 等级 |
| `energyStatus(roomName)` | 能量快照 {available, capacity, ratio} |
| `genName(role)` | 生成中文名 `{角色前缀}_{Game.time}[_序号]`（含 tick 内防重号） |

---

### 2.4 Migration — 版本迁移

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L2957-3079 |
| 启动阶段 | 主循环第一步 |
| 执行时机 | 仅在脚本版本号变化后执行一次 |

**迁移流程：**

```
Migration.run()
  ├── 读取 Memory.scriptVersion
  ├── 与 SCRIPT_VERSION (4) 比较
  ├── 若不一致:
  │   ├── 1. 状态评估报告
  │   │   ├── 主房间、RCL 等级/进度、能量、Bucket、GCL 进度
  │   │   └── 存活 Creep 统计（按角色）
  │   ├── 2. 内存迁移
  │   │   └── < v3: working 布尔值 → state 枚举 + 补充 homeRoom
  │   ├── 3. 清理通知标记 + 敌情标记
  │   ├── 4. 更新 Memory.scriptVersion
  │   └── 5. 发送启动通知（现状报告邮件）
  └── 若一致: 无操作
```

---

### 2.5 Logger — 日志系统

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L417-450 |
| 级别 | 0=全部 / 1=一般 / 2=警告 / 3=错误 |

**日志格式：** `[T{tick}] [表情] [模块名] 消息`  （可选 JSON 数据）

示例：
```
[T12345] ✅ [孵化] 🐣 采集者"采集_12345" 部件:8 | 成本:350 | 能量:500/800
[T12345] ⚠️ [房间] 🚨 3个敌对Creep！
[T12345] ❌ [主循环] 💥 "builder_12345"(builder)崩溃 | {"msg":"...","stack":"..."}
[T12345] 📊 [全局] 采集:2 | 定点:2 | 升级:2 | 建造:1 | 维修:0 | 运输:2 | 能量:680/1300
```

日志输出受 CPUGovernor 控制：**critical 模式** 跳过 INFO 级日志。

---

### 2.6 Notifier — 邮件通知

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L186-244 |
| 触发时机 | 主循环中被 RoomManager/其他模块调用 |

发送条件：
- 总开关 `CONFIG.NOTIFY.ENABLED` 必须为 true
- 同类通知有最小间隔控制 `CONFIG.NOTIFY.INTERVAL`（默认 1440 分钟 = 24 小时）。
  ⚠️ API 参数 groupInterval 单位是**分钟**且上限 1440，原值 172800 超出合法范围被 API 拒绝。
- 低能量通知额外受 `LOW_ENERGY_TICKS`（300）控制：能量需连续低于 15% 这么久才发信，避开孵化瞬时低谷。
- 通过 `Memory._ntf[key]` 记录上次发送时间实现去重。

**通知类型：**

| 方法 | 触发条件 | 备注 |
|------|---------|------|
| `onHostiles(count)` | 发现敌对 Creep | 敌人撤离后重置，下次入侵可再次通知 |
| `onHostilesClear()` | 敌人全部撤离 | 清除标记 |
| `onAllDead()` | 无任何存活 Creep | 有新 Creep 生成后自动重置 |
| `onCreepSpawned()` | Creep 成功孵化 | 清除全灭标记 |
| `onLowEnergy(energy, capacity)` | energyAvailable 连续 LOW_ENERGY_TICKS 低于 15% | 恢复后重置 |
| `onLowEnergyClear()` | 能量恢复到 15% 以上 | 清除低能标记 |
| `onStartup(report)` | 脚本升级后首次运行 | 发送详细现状报告

---

### 2.7 BodyBuilder — 动态身体部件生成

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L324-397 |
| 设计方法 | **模式重复法** — 按 role 定义基础模式组，根据可用能量自动缩放重复次数 |

**身体部件模式表：**

| 模式键 | 单组模式 | 每组能量 | 适用角色 |
|--------|---------|---------|---------|
| harvester | `[WORK,WORK,CARRY,MOVE]` | 350 | 走动式采集者 |
| staticHarvester | `[WORK,WORK,WORK,WORK,CARRY,MOVE]` | 550 | 定点采集者（高 WORK 密度） |
| upgrader | `[WORK,WORK,CARRY,MOVE]` | 350 | 升级者 |
| worker | `[WORK,CARRY,MOVE]` | 200 | 建造者/维修者/城墙维修者 |
| hauler | `[CARRY,CARRY,MOVE]` | 150 | 运输者 |
| claimer | `[CLAIM,MOVE]` | 650 | 占领者（预留） |
| remoteHauler | `[CARRY,CARRY,CARRY,CARRY,MOVE,MOVE]` | 400 | 远征运输 |

**最小可用身体（保底）：**

| 角色 | 保底身体 | 能量 |
|------|---------|------|
| claimer | `[CLAIM,MOVE]` | 650（必须保留 CLAIM 部件） |
| hauler | `[CARRY,CARRY,MOVE]` | 150 |
| remoteHauler | `[CARRY,CARRY,MOVE]` | 150 |
| 其他默认 | `[WORK,CARRY,MOVE]` | 200 |

**剩余能量追加：** 采集/升级角色（harvester/staticHarvester/upgrader）在满组后还会用剩余能量追加 `[WORK,MOVE]` 对和 `CARRY`，最大化容量利用。

### 2.8 Emergency — 采集者灭绝自救

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1334-1390 |
| 触发时机 | 采集者全部死亡 + 房间能量低于复活成本 |

**设计意图：** 采集者是房间经济唯一的能量入口。采集者全灭且剩余能量不足以复活一个时，房间永久死锁。Emergency 让任何带 WORK 部件的 Creep 临时改行挖矿，强行把 Spawn 能量推回复活线以上。

**核心方法：**

| 方法 | 功能 |
|------|------|
| `noHarvesters(cache)` | 检测房间有无存活采集者 |
| `isDeadlocked(cache)` | 判定是否处于死锁状态（无采集者 + 能量 < 复活成本） |
| `canRescue(creep)` | 检测该 Creep 是否有 WORK 和储物空间 |
| `run(creep, cache)` | 临时采集行为：挖满 → 送回 Spawn/Extension |

**行为流程：**
```
Emergency.run(creep)
  ├── 有空间 → 就近 Source 挖矿 (🆘挖)
  ├── 无空间且有能量 → 送至 Spawn/Extension (🆘送)
  └── 无空间无能量 → 原地等待 Source 刷新 (🆘等)
```

死锁检测在 `_runAllCreeps()` 中每房间每 tick 判定一次，所有非采集者角色在死锁解除前优先执行 Emergency 逻辑。

每个角色是一个数据对象，包含 `run(creep)` 方法。所有角色在 `_runAllCreeps()` 中被遍历调度，通过 `creep.memory.role` 匹配对应模块。

> **状态机：** 所有角色使用统一的状态枚举（`"harvesting"` ←→ `"working"/"upgrading"/"building"/"repairing"/"hauling"`），而非旧的 `working` 布尔值。

### 3.1 Harvester — 采集者（走动式）

| 属性 | 值 |
|------|-----|
| 角色名 | `harvester` |
| 启动阶段 | **第一阶段** — RCL1 即开始工作 |
| 启用条件 | 始终启用（`MIN_HARVESTERS: 1`） |
| 孵化顺序 | 优先级最高 |

**运行流程：**

```
Harvester.run(creep)
  ├── state === "harvesting"
  │   ├── 分配目标 Source（负载均衡: 选 Creep 数最少的 Source）
  │   ├── 移动到 Source
  │   └── 采集能量 (harvest)
  │
  └── state === "working"
      ├── 就近选择存储目标 (Spawn > Extension > Tower)
      ├── 移动到目标
      └── 存入能量 (transfer)
      └── 如果旁边有 Container → 改为存入 Container（本版本新增）
```

**身体部件模式：** `[WORK,WORK,CARRY,MOVE]` 重复，每组 350 能量，根据 `energyCapacityAvailable` 自动缩放。剩余能量追加 `[WORK,MOVE]` 对以用满容量。

**Source 分配策略：** 遍历所有 Source，对每个 Source 统计已分配的角色数（含 staticHarvester，两者共用同一负载计数器），选最少的那个。Creep 会将分配的 Source ID 保存在 `memory.sourceId` 中。

**故障处理：** 连续 10 tick 采集失败（ERR_NOT_ENOUGH_RESOURCES）→ 自动切换 Source。所有 Source 均不可用 → 小幅移动等待恢复，每 50 tick 重置尝试列表。

---

### 3.2 StaticHarvester — 定点采集者

| 属性 | 值 |
|------|-----|
| 角色名 | `staticHarvester` |
| 启动阶段 | **第二阶段** — RCL ≥ 2 时启用 |
| 启用条件 | RCL ≥ 2 且每个 Source 已有 Container |
| 孵化顺序 | 第 2 优先级 |

**运行逻辑：** 与 Harvester 共用 `run` 方法，但行为上有两个关键区别：

1. **身体部件偏重 WORK：** `[WORK,WORK,WORK,WORK,CARRY,MOVE]` 每组 550 能量，采集效率最高。
2. **存储逻辑自动切换：** 当检测到 Source 旁边有 Container 时，采集完成后直接存入 Container，不返回基地 → _定点不动，成为静态矿工_。
3. 同时在 Source 和 Container 之间绘制流线。

**设计意图：** 将"走动式采集"和"定点采集"分离。StaticHarvester 负责高效产出，Harvester 在 RCL1 过渡期负责确保能量供应。RCL2+ 后 Harvester 逐步减少，依赖 StaticHarvester + Hauler 的组合。

---

### 3.3 Hauler — 运输者

| 属性 | 值 |
|------|-----|
| 角色名 | `hauler` |
| 启动阶段 | **第二阶段** — RCL ≥ 2 时启用 |
| 启用条件 | RCL ≥ 2 且 `MIN_HAULERS: 1` |
| 孵化顺序 | 第 3 优先级（首次）、第 7 优先级（补充） |

**运行流程（Hauler._pickup — 取能优先级）：**

```
Hauler._pickup(creep)
  ├── 1. 地上掉落物（> PICKUP_THRESHOLD 50）
  ├── 2. Container 取能（> WITHDRAW_THRESHOLD 200）
  ├── 3. Terminal 取化合物补给 Lab（RCL6+）
  ├── 4. Storage 取能
  └── 5. RCL<3 且无 Container 时的 fallback → 直接去 Source 采集
```

**投放优先级（findDepositTarget）：**

```
  ├── 0. Controller 附近 Container（能量 < 500 时优先）
  ├── 1. Spawn
  ├── 2. Extension
  ├── 3. Tower（能量 < TOWER_MIN_ENERGY 500）
  ├── 4. Storage
  └── 5. Link（最后的缓冲）
```

**非能量资源处理：** Hauler 身上若有非能量资源（化合物等），优先送入 Lab，其次 Terminal/Storage，无处可放时丢弃（drop）。

**设计意图：** 在 StaticHarvester 将能量存入 Container 后，Hauler 负责将 Container 的能量搬运到基地。分离了"采集"和"运输"两个职责。

**身体部件模式：** `[CARRY,CARRY,MOVE]` 重复，每组 150 能量。高 Carry 容量适合运输。

---

### 3.4 Upgrader — 升级者

| 属性 | 值 |
|------|-----|
| 角色名 | `upgrader` |
| 启动阶段 | **第一阶段** — RCL1 即开始工作 |
| 启用条件 | 始终启用（`MIN_UPGRADERS: 1`） |
| 孵化顺序 | 第 4 优先级（首次）、第 9 优先级（补充，上限 4） |
| 兜底作用 | 其他角色无任务时也会回退到升级 |

**运行流程：**

```
Upgrader.run(creep)
  ├── LabManager 标记强化目标 (_boostLabId) → 先去 Lab 报到
  │   └── 移动到 Lab → 等待 boostCreep 完成 → 继续正常流程
  │
  ├── state === "harvesting"
  │   ├── 1. Container 取能量（优先）
  │   ├── 2. 从 Storage 取
  │   ├── 3. Controller Link 取能（RCL5+ 自动送能到 Controller 旁）
  │   ├── 4. 有 WORK 部件 → 自己去挖（不抽 Spawn）
  │   ├── 5. Spawn/Extensions（能量比例高于阈值时才允许，且留够复活采集者的本钱）
  │   └── 移动到目标取能
  │
  └── state === "upgrading"
      └── 移动到 Controller 并 upgradeController
```

**取能策略（withdraw 函数）的关键设计：**

```
withdraw(creep)
  ├── 1. Container（> 200 能量）
  ├── 2. Storage
  ├── 3. Controller Link（RCL5+）
  ├── 4. 有 WORK → 自行挖矿（避免抽干 Spawn）
  └── 5. Spawn/Extension（仅在能量充裕 + 留足复活金时）
```

⚠️ 顺序 4 在 5 之前：有 WORK 的角色先去挖矿而不是抢 Spawn 的能量，采集者死光后 Spawn 还有钱复活。

**兜底行为：** Builder、Repairer、WallRepairer 在没有目标工作时，都会回退到 `"upgrading"` 状态，成为临时升级者。

**身体部件模式：** `[WORK,WORK,CARRY,MOVE]` 重复，每组 350 能量。剩余能量追加 `[WORK,MOVE]` 对。

---

### 3.5 Builder — 建造者

| 属性 | 值 |
|------|-----|
| 角色名 | `builder` |
| 启动阶段 | **第一阶段** — RCL1 即开始工作 |
| 启用条件 | 有 ConstructionSite 且 `MAX_BUILDERS: 3` |
| 孵化顺序 | 第 5 优先级 |

**运行流程：**

```
Builder.run(creep)
  ├── state === "harvesting" → 取能（withdraw）
  │
  ├── state === "building"
  │   ├── 按优先级选择工地:
  │   │   EXTENSION > TOWER > STORAGE > SPAWN > CONTAINER > ROAD > WALL > RAMPART
  │   ├── 移动到工地
  │   └── 建造 (build)
  │   │
  │   └── 无工地 → state 切换为 "upgrading"（回退）
  │
  └── state !== harvesting 且 !== building → upgradeFallback
```

**关键状态逻辑：** Builder 处于非 HARVESTING 的任何状态都视为"满载干活"，避免无工地时被错误切到取能分支而空转。

**孵化条件：** 房间内有工地时才投入 Builder 配额（`MAX_BUILDERS: 3`），无工地时 Builder 数量降为 0。

**身体部件模式：** `[WORK,CARRY,MOVE]` 重复，每组 200 能量。

---

### 3.6 Repairer — 维修者

| 属性 | 值 |
|------|-----|
| 角色名 | `repairer` |
| 启动阶段 | **第一阶段** — RCL1 即开始维修 |
| 启用条件 | 有损坏道路/容器时 |
| 孵化顺序 | 第 6 优先级 |
| 数量上限 | `MAX_REPAIRERS: 2` |

**运行流程：**

```
Repairer.run(creep)
  ├── state === "harvesting" → 取能（withdraw）
  │
  ├── state === "repairing"
  │   ├── 筛选可维修目标（从 Helpers.findToRepair 获取）:
  │   │   ├── 道路: h/hmax < ROAD_THRESHOLD (0.5)
  │   │   ├── 容器: h/hmax < STRUCTURE_THRESHOLD (0.5)
  │   │   ├── 城墙/壁垒: h/hmax < WALL_THRESHOLD (0.0001)
  │   │   └── 其他建筑: h/hmax < STRUCTURE_THRESHOLD (0.5)
  │   ├── 排序: 最严重的优先
  │   ├── 优先修道路和容器
  │   ├── 移动到目标
  │   └── 维修 (repair)
  │   │
  │   └── 无损坏目标 → state 切换为 "upgrading"（回退）
  │
  └── state 非 harvesting 且非 repairing → upgradeFallback
```

**设计意图：** 专注维修道路和容器，城墙维修交由专门的 WallRepairer。

**身体部件模式：** `[WORK,CARRY,MOVE]` 重复，每组 200 能量。

---

### 3.7 WallRepairer — 城墙维修者

| 属性 | 值 |
|------|-----|
| 角色名 | `wallRepairer` |
| 启动阶段 | **第三阶段** — RCL ≥ 3 时启用 |
| 启用条件 | RCL ≥ 3 且有墙/rampart 低于阈值 |
| 孵化顺序 | 第 8 优先级 |
| 数量上限 | `MAX_WALL_REPAIRERS: 1` |

**运行流程：**

```
WallRepairer.run(creep)
  ├── state === "harvesting"
  │   ├── 从 Storage 取能（优先）
  │   └── 没有 Storage: 从 Container 取能
  │
  ├── state === "repairing"
  │   ├── 筛选: h/hmax < WALL_MAX_RATIO (0.001 = 0.1% HP) 的墙/壁垒
  │   ├── 按 HP 从低到高排序
  │   ├── 移动到目标
  │   └── 维修 (repair)
  │   │
  │   └── 无目标 → state 切换为 "upgrading"（回退）
  │
  └── state 非 harvesting 且非 repairing → upgradeFallback
```

**阈值说明：**
- `WALL_MAX_RATIO` (0.001) — 这是 WallRepairer **筛选标准**，只处理 HP < 0.1% 的墙/壁垒。
  ⚠️ 区别于原实现同时用 WALL_THRESHOLD 和 WALL_MAX_RATIO 两个方向相反的阈值导致墙永远修不上去的 Bug，v4.0 已统一为只按 `WALL_MAX_RATIO` 筛选。

**设计意图：** 城墙需要海量能量来维修（RCL1-3 不需要），因此延迟到 RCL3+ 有 Storage 后才启动。

**身体部件模式：** `[WORK,CARRY,MOVE]` 重复，每组 200 能量。

---

### 3.8 Claimer — 远征占领者

| 属性 | 值 |
|------|-----|
| 角色名 | `claimer` |
| 启动阶段 | **P5** — RCL6+ 启用（由 RemoteManager 控制） |
| 启用条件 | 存在已注册的远程房间（remote_* Flag） |
| 数量上限 | `MAX_CLAIMERS: 1` |

**运行流程：**

```
Claimer.run(creep)
  ├── 未到达目标房间 → 跨房间移动到 targetRoom
  │
  ├── 到达目标房间
  │   ├── reserveController（续约）
  │   ├── 成功 → 更新 Memory.remoteRooms[roomName].reservedUntil
  │   └── 失败（ERR_NOT_IN_RANGE）→ 移动到 Controller
```

**身体部件：** `[CLAIM, MOVE]` × 按容量缩放。⚠️ 最小保底必须保留 `[CLAIM, MOVE]`，否则无法 reserve。

---

### 3.9 RemoteHauler — 远征运输者

| 属性 | 值 |
|------|-----|
| 角色名 | `remoteHauler` |
| 启动阶段 | **P5** — RCL6+ 启用（由 RemoteManager 控制） |
| 启用条件 | 存在已注册的远程房间且 Storage 能量 ≥ MIN_STORAGE_ENERGY (30000) |
| 数量上限 | `MAX_REMOTE_HAULERS: 2` |

**运行流程：**

```
RemoteHauler.run(creep)
  ├── state === "hauling"（满载）
  │   ├── 在 homeRoom → 存入 Storage（优先）或 findDepositTarget
  │   └── 不在 homeRoom → 跨房间回家
  │
  └── state === "harvesting"（空载）
      ├── 未到达目标房间 → 跨房间移动到 targetRoom
      ├── 在目标房间:
      │   ├── 1. 捡地上掉落（> PICKUP_THRESHOLD）
      │   ├── 2. 从容器取能（> HAUL_AMOUNT_MIN 500）
      │   └── 3. 无资源 → 回家避免空等
      └── 回家路径: room.findExitTo + findClosestByRange
```

**身体部件：** `[CARRY,CARRY,CARRY,CARRY,MOVE,MOVE]` 重复，每组 400 能量。高 Carry 容量适合一次搬更多。

---

## 4. 管理层模块

### 4.1 RoomManager — 房间管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L2076-2247 |
| 启动阶段 | 主循环第 5 步（紧接角色统计之后） |
| 依赖 | RoomCache、CPUGovernor、Notifier |

**运行流程：**

```
RoomManager.run(counts)
  ├── 1. 能量预警 + 邮件通知
  │   ├── energyAvailable / capacity < 0.15
  │   │   ├── 记录低能量起始 tick（Memory._lowEnergySince）
  │   │   ├── 持续 ≥ LOW_ENERGY_TICKS (300) → 发送 Notifier.onLowEnergy()
  │   │   └── 每 100 tick 控制台输出 🔴 红色预警
  │   ├── 0.15 < ratio < 0.30 → 🟡 黄色预警
  │   └── ratio 恢复 > 0.15 → 清除低能标记 + Notifier.onLowEnergyClear()
  │
  ├── 2. RCL 进度报告（每 100 tick）
  │   ├── 显示当前 RCL、进度%、降级剩余 tick
  │   └── ticksToDowngrade < 5000 → ⚠️ 警告
  │
  ├── 3. 敌对检测
  │   ├── 检测 RoomCache 中 hostiles 列表
  │   ├── 首次发现 → 控制台日志 + Notifier.onHostiles()
  │   ├── 自动激活安全模式（safeModeAvailable > 0 且未激活时）
  │   ├── 更新 Memory._hadHostiles 追踪敌情
  │   └── 敌人全部撤离 → Notifier.onHostilesClear()
  │
  ├── 4. Spawn 健康检查
  │   └── Spawn.hits < hitsMax * 0.5 → 控制台告警
  │
  ├── 5. Container 自动放置（RCL2+）
  │   ├── _planControllerContainer()
  │   │   └── Controller 3 格内无 Container → 自动放置工地
  │   └── _planSourceContainers()
  │       └── 每个 Source 1 格内无 Container → 自动放置工地（选离 Spawn 最近的位置）
  │
  └── 6. 调试可视化（LOG_LEVEL <= 0 且 CPU 非 critical 时）
      └── Source ⛏️标记、Spawn 🐣标记、RCL 🏛️标记
```

---

### 4.2 TowerManager — 炮塔管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1694-1758 |
| 启动阶段 | 主循环第 6 步（道路规划之后） |
| 依赖 | RoomCache、CPUGovernor |
| 优先级 | 攻击 > 治疗 > 维修 |

**运行流程：**

```
TowerManager.run()
  ├── 遍历所有 Tower (RoomCache.get().towers)
  │   （能量 < 10 的 Tower 跳过）
  │
  ├── 1. 攻击 (ATTACK) — 优先级最高
  │   ├── 检查 hostiles 列表
  │   ├── 目标评分: HEAL * 3 + (ATTACK + RANGED_ATTACK) * 2
  │   ├── 选择最高分目标（优先杀治疗单位）
  │   └── 攻击 → 日志
  │
  ├── 2. 治疗 (HEAL) — 仅当无敌人时执行
  │   ├── 筛选 hits < hitsMax 的己方 Creep
  │   └── 治疗 HP% 最低且 < 80% 的 Creep
  │
  └── 3. 维修 (REPAIR) — 仅当能量 > TOWER_MIN_ENERGY (500) 且 CPU 非 critical
      ├── 优先修道路（hits < 50%）
      └── 其次修容器（hits < 50%）
```

**CPU 降级联动：** critical 模式时 `shouldSkipTowerUtil()` 返回 true → 跳过维修步骤，保留攻击和治疗。

---

### 4.3 SpawnManager — 孵化管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1398-1690 |
| 启动阶段 | 主循环第 7 步（所有 Creep 跑完之后） |
| 依赖 | RoomCache、BodyBuilder、counts、Notifier |

**设计特色：**
- **队列孵化**替代贪心链：一次构建完整优先级队列，按 P0→P1→P2→P3 层级依次匹配。
- **预生成替换**（pre-spawn）：Creep 剩余寿命 < PRE_SPAWN_TICKS (50) 时提前孵化替代者，允许临时超编（+1）保证无缝衔接。
- **多 Spawn 并行**：遍历所有 Spawn，每个空闲 Spawn 从队列弹出可负担的孵化任务。
- **能量分层预留**：P0 角色（采集/升级/运输/定点）未满足前禁止 P2+ 角色消耗能量；P1 预生成未完成前为 P2+ 保留 300 能量。
- **立体降级**：满容量 → 缩水 → 角色最小保底 → 跳过，四级回退策略。
- **角色上限映射**：用显式 `MAX_KEYS` 表（而非字符串拼接），解决原实现 `MAX_WALLREPAIRERS` 拼写错误导致上限无效的 Bug。

**优先级队列（数字越小越优先）：**

| 层级 | 优先级 | 角色 | 触发条件 |
|------|--------|------|---------|
| **T0 关键** | 0 | harvester | < MIN_HARVESTERS 或采集比例 < 25% |
| | 0 | staticHarvester | RCL ≥ 2 + Source Container 就绪 + 数量 < Source 数 |
| | 0 | hauler | RCL ≥ 2 且 < MIN_HAULERS |
| | 0 | upgrader | < MIN_UPGRADERS |
| **T1 预生成** | 1 | (关键角色) | 剩余寿命 < 50t 的 harvester/staticHarvester/upgrader/hauler |
| | 1 | claimer | 有远程房间且 < MAX_CLAIMERS |
| **T2 填充** | 2 | builder | 有工地且 < MAX_BUILDERS |
| | 2 | repairer | 有损坏道路且 < MAX_REPAIRERS |
| | 2 | upgrader | < MAX_UPGRADERS（填充） |
| | 2 | remoteHauler | 有远程房间且 < MAX_REMOTE_HAULERS |
| **T3 补充** | 3 | hauler | < MAX_HAULERS（填充） |
| | 3 | wallRepairer | RCL ≥ 3 且 < MAX_WALL_REPAIRERS |

**孵化流程：**

```
SpawnManager.run(counts)
  ├── _buildQueue()
  │   ├── T0: 关键最小数 (P0)
  │   │   ├── harvester (< MIN 或采集比例不足)
  │   │   ├── staticHarvester (RCL2+ 且 Container 就位)
  │   │   ├── hauler (RCL2+)
  │   │   └── upgrader (< MIN)
  │   ├── T1: 预生成替换 (P1) — 即将死亡的关键 Creep
  │   ├── T2: 填充到上限 (P2)
  │   │   ├── builder（有工地时）
  │   │   ├── repairer（有损坏道路时）
  │   │   ├── upgrader（填充）
  │   │   └── remoteHauler（远程房间启用时）
  │   └── T3: 补充 (P3)
  │       ├── hauler（填充）
  │       └── wallRepairer（RCL3+）
  │
  └── 遍历所有 Spawn
      └── _popAffordable(queue, spawn) → 从队列弹出第一个可承受的任务
          ├── 满容量身体 → cost 可用能量?
          │   ├── 是 → 使用满配
          │   └── 否 → 缩水（按可用能量重算）
          │       └── 缩水不够 → 最小身体保底（按角色区分）
          │           └── 连保底都不够 → 跳过
          └── _executeSpawn(spawn, item)
              ├── 上限检查（预生成允许 +1 超编）
              ├── 初始 Memory: { role, state: "harvesting", homeRoom, targetRoom? }
              ├── spawnCreep()
              ├── 成功 → counts[role] +1（多 Spawn 同步）
              └── Notifier.onCreepSpawned()
```

---

### 4.4 LinkManager — Link 网络管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1762-1797 |
| 启动阶段 | 主循环第 4 步（TowerManager 之后） |
| 启用条件 | RCL ≥ 5 且房间内 Link 数量 ≥ 2 |

**运行流程：**

```
LinkManager.run(cache)
  ├── 找到 Controller Link（距 Controller 3 格内的 Link）
  ├── 找到 Source Links（距 Source 2 格内的 Link，排除 Controller Link）
  └── 每个 Source Link:
      ├── cooldown > 0 ? 跳过
      ├── 存量 < SEND_THRESHOLD (400) ? 跳过
      ├── Controller Link 空闲容量 < 100 ? 跳过
      ├── Controller Link 存量 ≥ RECEIVE_MAX (600) ? 跳过
      └── 发送：src.transferEnergy(ctrlLink)
```

**设计意图：** RCL5+ 解锁 Link 后，Source 旁的 Link 自动将能量传送到 Controller 旁的 Link，Upgrader 直接从 Controller Link 取能，大幅减少搬运工往返路程。

### 4.5 MarketManager — 市场交易管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1801-1891 |
| 启动阶段 | 主循环第 4 步（LinkManager 之后） |
| 启用条件 | RCL ≥ 4 且 Terminal + Storage 均已建造 |

**运行流程：**

```
MarketManager.run(cache, homeRoom)
  ├── 节流：每 TRADE_INTERVAL (100) tick 扫一次市场
  │   ⚠️ 记账节流 = 防止每 tick 调用 Game.market.getAllOrders()
  │
  ├── _buyEnergy()
  │   ├── Storage 能量 < ENERGY_BUY_THRESHOLD (10000)?
  │   ├── Terminal 留足 TERMINAL_ENERGY_RESERVE (5000)?
  │   ├── 找 sell orders: 能量单价在 [ENERGY_BUY_MIN, ENERGY_BUY_MAX]
  │   ├── 选最低价订单
  │   ├── Credits 够付？
  │   └── Game.market.deal()
  │
  └── _sellMinerals()
      ├── Storage 填充率 > STORAGE_SELL_THRESHOLD (80%)?
      ├── 从 Terminal 库存中找可卖矿物（排除能量，存量 ≥ 100）
      ├── 找 buy orders: 单价 ≥ MINERAL_SELL_MIN
      └── 选最高价订单 → deal()
```

**安全设计：**
- Terminal 保留最小能量（TERMINAL_ENERGY_RESERVE），确保有运费支付能力。
- 只能卖 Terminal 里的货（原实现按 Storage 库存算量，deal 时从 Terminal 取货直接报错）。
- 交易记录存储在 `Memory._trades`（最近 20 条），便于调试。

### 4.6 LabManager — 实验室强化管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1965-2072 |
| 启动阶段 | 主循环第 4 步（MarketManager 之后） |
| 启用条件 | RCL ≥ 6 |

**运行流程：**

```
LabManager.run(cache, homeRoom)
  ├── 1. _planLabs() — 自动放置 Lab 工地
  │   ├── 现有 Lab + 工地 < MIN_LABS (3)？
  │   ├── 在 Controller 周围 LAB_RANGE (3) 格内搜索空地
  │   └── 放置 Lab 工地
  │
  ├── 2. _buyCompounds() — 从市场购买化合物（每 200t）
  │   ├── 检查 Terminal + Storage + Labs 中已有存量
  │   ├── 存量 < COMPOUND_BUY_AMOUNT (300)?
  │   ├── 找 sell orders: 单价 ≤ COMPOUND_BUY_MAX (0.5)
  │   └── Game.market.deal()
  │
  └── 3. _boostUpgraders() — 强化升级者
      ├── Storage 能量 ≥ BOOST_MIN_ENERGY (20000)?
      ├── 遍历 Lab，找有化合物且能量充足的
      ├── 找最近的未强化 Upgrader（WORK 部件数 > 0）
      ├── 标记 _boostLabId → Upgrader 自己走过来
      └── lab.boostCreep(creep) → 成功则标记 _boosted = true
```

**关键设计：**
- 标记 `_boostLabId` 而不是直接 `moveTo`：LabManager 的调用时机在 _runAllCreeps 之前或之后，直接移动会被角色自身逻辑覆盖。
- 每个 Lab 每 tick 只招呼**一个** Upgrader，避免全体升级者集体罢工去排队。
- 强化用的化合物（GH/UH/LH）由 Hauler 从 Terminal 运到 Lab。

### 4.7 RemoteMonitor — 远程 Flag 探测

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1895-1936 |
| 启动阶段 | 主循环第 2b 步（RoomCache 之后） |

**运行流程：**

```
RemoteMonitor.run(homeRoom)
  ├── 扫描所有名称以 "remote_" 开头的 Flag
  ├── 新房间 → 注册到 Memory.remoteRooms
  │   └── { flagName, reservedUntil, sources[], containerIds[] }
  └── 已删除 Flag 的房间 → 清除记录
```

`refreshCache(roomName)` — 如果远程房间在当前 tick 可见（Game.rooms[roomName]），调用 RoomCache.refresh() 并更新 Memory.remoteRooms 中的 Source/Container 信息。

### 4.8 RemoteManager — 远征管理器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L1940-1961 |
| 启动阶段 | 主循环第 5 步（RoomManager 之后） |

**运行流程：**

```
RemoteManager.run(homeRoom)
  ├── Storage 能量 < MIN_STORAGE_ENERGY (30000)? → 跳过出征
  └── 遍历所有远程房间
      └── RemoteMonitor.refreshCache(roomName) → 刷新数据
          └── 每 200 tick 输出状态日志
```

### 4.9 RoadPlanner — 道路自动规划

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L2345-2412 |
| 启动阶段 | 主循环第 5 步（RoomManager 之后） |
| 执行频率 | 每 200 tick 一次 |

**运行流程：**

```
RoadPlanner.run()
  ├── 构建 CostMatrix（已有道路=1，墙壁=255，建筑=255，容器/路障可穿越）
  ├── 找三条关键路径:
  │   ├── 每个 Source → Spawn（PathFinder）
  │   └── Spawn → Controller（PathFinder）
  ├── 去重 + 检查位置合法性
  └── room.createConstructionSite(STRUCTURE_ROAD)
```

**设计意图：** 自动在 Source→Spawn→Controller 主干道上铺设道路，加速 Creep 移动速度。每 200 tick 规划一次以覆盖新区。

### 4.10 StatsTracker — 统计追踪器

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L2254-2336 |
| 启动阶段 | 主循环第 8 步（SpawnManager 之后） |
| 依赖 | RoomCache、Memory.stats |

**运行流程与存储的数据：**

```
StatsTracker.record(roomName)
  ├── 1. rclLog (最近 50 tick)
  │   └── {t, p, total, level, ticksToDowngrade}
  │   └── 去重：相同 progress 且间隔 < 5t 不记录
  │
  ├── 2. gclLog (最近 100 tick)
  │   └── {t, p, total, level}
  │   └── 去重：相同 progress 且间隔 < 10t 不记录
  │
  ├── 3. energyLog (最近 100 tick)
  │   └── {t, e, c} — 每 tick 记录
  │
  └── 4. spawnActivity
      └── {ticks, spawning} — 计数
```

**分析功能：**
- `energyRate()` — 最近 100 tick 前后半段能量的平均差值 ÷ 时间差 → 净能率
- `rclETA()` / `gclETA()` — 基于最近 10 个样本的线性回归 → 升级 ETA
- `spawnUptime()` — Spawn 忙碌 tick 占比 → 使用率

---

## 5. 可视化模块

### 5.1 VisConfig — 可视化 Flag 开关

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L2425-2436 |
| 作用 | 根据房间内 Flag 返回可视化级别，无需改代码 |

**级别定义：**

| 返回值 | Flag | 效果 |
|--------|------|------|
| 0 | `viz_off` | 关闭全部可视化（CPU 零开销） |
| 1 | `viz_min` | 仅左上方面板 + 底部警报条 |
| 2 | （无 flag） | 默认完整可视化 |
| 3 | `viz_debug` | 完整可视化 + 面板标题显示"调试" |

### 5.2 Visuals — 信息面板与可视化

| 属性 | 值 |
|------|-----|
| 定义位置 | `main.js` L2444-2947 |
| 启动阶段 | 主循环最后一步（第 9 步） |
| 依赖 | RoomCache、StatsTracker、VisConfig、CPUGovernor |
| CPU 开销 | RoomVisual 本身零 CPU，但逻辑有微量开销 |

**绘制内容（按绘制顺序）：**

```
Visuals.run()
  ├── VisConfig.level() 判定 → 0 或 CPU critical 时直接跳过
  │
  ├── 1. _infoPanel() — 左上角信息面板
  │   ├── 标题行: ▛ 基地 T:{tick} 🟢/🟡/🔴 [+调试]
  │   ├── GCL 进度: 🌐 GCL{n} ██░░ 32.1%  ⏱ ETA (Kt 格式)
  │   ├── RCL 进度: 🏛 RCL{n} ██░░ 58%    ⏱ ETA ⚠降级
  │   ├── 能量: ⚡ 能量 ██░░ 680/1300  (颜色: 🟢>50% 🟡20-50% 🔴<20%)
  │   ├── 孵化: 🐣孵化中{n}t / ⏸空闲(使用率{n}%)
  │   ├── 净能率: 📈 +3.2/t  (🟢正值 🔴负值)
  │   ├── 角色统计: 👥 8人 采:3 升:2 建:1 修:1 运:1 墙:0 占:0 远:0
  │   └── 资源统计: 📦 {container+storage}  🏗 {sites}  🛡 {towers}  💾 {bucket}
  │
  ├── 2. _phasePanel() — 阶段面板（面板右侧）
  │   ├── 阶段标识 P1-P5 + 阶段名称
  │   ├── 整体进度条 + 完成百分比
  │   └── 各项目标: ✅ / 🔄 (百分比/计数) / ⬜
  │
  ├── 3. _energyFlow() — 能量流动画
  │   ├── 🟡 Source → Container（虚线 + 往复粒子）
  │   ├── 🟢 Container → Spawn（虚线 + 往复粒子）
  │   ├── 🔵 Storage/Spawn → Controller（点线 + 往复粒子）
  │   ├── Storage 能量标注 (K 格式)
  │   └── Container 能量标注
  │
  ├── 4. _controllerBar() — Controller 进度条
  │   └── 青色 RCL{n} {进度%} ⏱ETA
  │
  ├── 5. _constructionBars() — 工地进度条
  │   └── 黄色 {完成度%}
  │
  ├── 6. _sourceLabels() — Source 标记
  │   ├── ⛏️N → 传统采集者数量（无 Container）
  │   └── 📦N → 定点采集者数量（有 Container）
  │   └── Source→Container 虚线连线
  │
  ├── 7. _spawnBar() — Spawn 孵化进度
  │   ├── 孵化中 → 紫色进度条 + 角色名 + 剩余 tick
  │   └── 空闲 → 🐣 待命
  │
  └── 8. _alerts() — 底部警报条（脉冲动画）
      └── 按严重程度: 💀全灭 > ⚠无采集者 > ⚔N敌人 > 🔴能量 > ⏬降级 > ⚠无升级者 > ⏸Spawn空闲 > 💾Bucket低/极低
```

---

## 6. 主循环时序

完整的主循环执行顺序（`module.exports.loop`）：

```
Step  ┆ 模块                      ┆ 功能                               ┆ 条件
──────┼───────────────────────────┼────────────────────────────────────┼─────────────────
  1   ┆ Migration.run()           ┆ 版本迁移（仅脚本升级后的首 tick）   ┆ 版本变化时
  2   ┆ cleanMemory()             ┆ 清理死亡 Creep 的内存               ┆ 每 tick
  3   ┆ RoomCache.refresh()       ┆ 缓存房间数据（一次 find 供全 tick） ┆ 每 tick
  4   ┆ RemoteMonitor.run()       ┆ 扫描 remote_* Flag 注册远程房间     ┆ 每 tick
  5   ┆ countCreepsByRole()       ┆ 全局遍历存活角色数量                ┆ 每 tick
  6   ┆ RoomManager.run()         ┆ 房间监测 + 预警 + Container 自动放置 ┆ 每 tick
  7   ┆ RemoteManager.run()       ┆ 远程房间数据刷新 + 状态日志         ┆ 每 tick
  8   ┆ RoadPlanner.run()         ┆ 自动规划 Source→Spawn→Controller 路 ┆ 每 200t
  9   ┆ TowerManager.run()        ┆ 炮塔: 攻击→治疗→维修              ┆ 每 tick
 10   ┆ LinkManager.run()         ┆ RCL5+ Link 自动输电                ┆ 每 tick
 11   ┆ MarketManager.run()       ┆ RCL4+ Terminal 能量买入/矿物卖出   ┆ 每 100t 节流
 12   ┆ LabManager.run()          ┆ RCL6+ Lab 放置 + 购买 + 强化       ┆ 每 tick
 13   ┆ _runAllCreeps()           ┆ 遍历 Creep 执行角色逻辑            ┆ 每 tick
  │    ├── Emergency 死锁检测     ┆ 采集者灭绝时临时挖矿自救           ┆ 每 tick
  │    └── 各角色 run()           ┆ 按 role 分发到对应模块             ┆ 每 tick
 14   ┆ SpawnManager.run()        ┆ 优先级队列 + 预生成替换 + 孵化    ┆ 每 tick
 15   ┆ StatsTracker.record()     ┆ 记录 stats (energy/RCL/GCL/Spawn)  ┆ 每 tick
 16   ┆ _report()                 ┆ 控制台单行摘要输出                  ┆ 每 tick
 17   ┆ Visuals.run()             ┆ 面板 + 阶段面板 + 流线 (critical跳过)┆ 每 tick
```

**异常保护：** 整个主循环包裹在 try/catch 中。`__loopErrorCount` 追踪连续崩溃次数，5 tick 后触发 `Game.notify()` 邮件告警。

---

## 7. 模块启动阶段总览

以下按 5 个发展阶段划分各模块/角色的启动时机和目标体系。游戏内面板右侧的 **阶段面板** 会实时显示当前处于哪个阶段及各目标完成进度。

---

### 🔰 P1 — 基础·初始积累（RCL 1）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 2 | ✅ 已实现 | Controller 进度达到 100% |
| 采集者就位 | ✅ 已实现 | 至少 1 个 Harvester 存活工作 |
| 建造 Extensions | ✅ 已实现 | 建造至少 5 个 Extension 扩展容量 |
| 能量正增长 | ✅ 已实现 | 净能率 > 0，经济自持 |

**实际运行中的角色：**

| 模块 | 说明 |
|------|------|
| **Harvester** | 走动式采集者，直接挖矿→送回 Spawn，RCL1 唯一能量来源 |
| **Upgrader** | 升级 Controller 冲 RCL2，至少 1 个常驻 |
| **Builder** | 有工地时自动建造，优先级 EX > TOWER > STORAGE |
| **Repairer** | 道路/容器破损时自动维修 |
| **TowerManager** | 炮塔防御（有 Tower 就开始） |
| **RoomManager** | 房间监测预警从一开始就运行 |

> 阶段目标：尽快积累 300 能量升级 RCL2，进入容器经济。

---

### 📦 P2 — 容器·定点经济（RCL 2–3）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 3 | ✅ 已实现 | Controller 进度达到 100% |
| Container 覆盖 | ✅ 已实现 | 每个 Source 旁至少放好 1 个 Container |
| 定点采集就位 | ✅ 已实现 | 每个 Source 配 1 个 StaticHarvester |
| 搬运工就位 | ✅ 已实现 | 至少 1 个 Hauler 在运输线上 |
| 建造 Storage | ✅ 已实现 | Storage 建成，能量开始缓冲 |

**新启用模块：**

| 新启用模块 | 说明 |
|-----------|------|
| **StaticHarvester** | 在每个 Source 旁放 Container 后，定点采集者取代走动式采集者 |
| **Hauler** | 专职搬运工，连接 Container→基地 |
| 🔄 **Harvester → 减少** | 逐步被 StaticHarvester 替代，从主力变为补充/兜底 |

> 阶段特征：采集→Container→Hauler→Spawn/Storage 的完整流水线形成。Harvester 不再需要走回基地，效率大幅提升。

---

### 🛠 P3 — 建造·设施完善（RCL 3–4）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 4 | ✅ 已实现 | Controller 进度达到 100% |
| 建造炮塔 | ✅ 已实现 | 至少 1 个 Tower 覆盖基地 |
| 城墙/路障 | ✅ 已实现 | 建造城墙和路障围护关键设施 |
| Storage 落成 | ✅ 已实现 | Storage 建成 |
| 储能达标 10K | ✅ 已实现 | Storage 积累 10,000 能量缓冲 |

**新启用模块：**

| 新启用模块 | 说明 |
|-----------|------|
| **WallRepairer** | 城墙/路障维修（RCL3+ 有 Storage 后启动） |
| **Tower 维修** | Tower 在能量充裕时修补墙/道路 |

> 阶段特征：RCL3 解锁 Tower/Wall/Rampart，从纯经济建设转向设施完善 + 初步防御。

---

### ⚗ P4 — 成长·生产升级（RCL 4–6）

| 目标 | 状态 | 说明 |
|------|------|------|
| RCL 升到 6 | ✅ 已实现 | Controller 进度达到 100% |
| 炮塔完善 | ✅ 已实现 | 建造至少 3 个 Tower 形成防御火力网 |
| 储能达标 50K | ✅ 已实现 | Storage 积累 50,000 能量供扩张使用 |
| 城墙维修者 | ✅ 已实现 | WallRepairer 自动维持城墙耐久 |
| 矿物/交易系统 | ✅ 已实现 | Terminal + Market 自动买卖能量和矿物 |

**实际运行中的角色（P1-P3 持续运作 + 新增）：**

| 模块 | 说明 |
|-----------|------|
| **WallRepairer** | 城墙/路障持续维修（该阶段成为常驻角色） |
| **所有 P1-P3 角色** | 持续运作，产能爬坡 |
| **MarketManager** | RCL4+ Terminal 就绪后自动交易：Storage 能量不足时低价买入，Storage 填充率 > 80% 时卖出多余矿物 |
| **LinkManager** | RCL5+ Link 就绪后自动 Source→Controller 远程送能 |

> 阶段特征：从生存建设转向产能爬坡，储能攀升至 50K 为跨房间扩张做储备。矿物交易系统运作后可以赚取 Credits。

---

### 🚀 P5 — 扩张·跨房间（RCL 6+）

| 目标 | 状态 | 说明 |
|------|------|------|
| 远征采集 | ✅ 已实现 | 放置 `remote_` 前缀 Flag → RemoteMonitor 自动注册房间 → RemoteHauler 跨房间搬运能量 |
| Claimer 就绪 | ✅ 已实现 | Claimer 自动跨房间续约 Controller（reserveController） |
| Link 网络 | ✅ 已实现 | RCL5+ LinkManager 自动 Source→Controller 输电（RCL 6 前也可用） |
| 多房间运营 | ✅ 已实现 | RoomCache 按 roomName 索引，支持任意多个房间 |

---

## 附录 A：数据依赖关系图

```
┌──────────────────────────────────────────────────────────────┐
│                  Migration.run()                              │
│               (Memory.scriptVersion)                          │
└──────────────────────────────────────────────────────────────┘
                           │
┌──────────────────────────────────────────────────────────────┐
│                  cleanMemory()                                │
│              (Memory.creeps ⟂ Game.creeps)                    │
└──────────────────────────────────────────────────────────────┘
                           │
┌──────────────────────────────────────────────────────────────┐
│  RoomCache.refresh(room)  ────  room.find() ×6                │
│       │                                                       │
│       ├── spawns / extensions / towers / containers           │
│       ├── storage / terminal / links / labs                   │
│       ├── roads / walls / ramparts / myStructures             │
│       ├── sources / sites / dropped / hostiles / myCreeps     │
│       └── controller / rcl / energyAvailable / capacity       │
└───────┬──────────────────────────────────────────────────────┘
        │
        ├──────────────────┐
        ▼                  ▼
┌─────────────────┐  ┌──────────────────────┐
│ RemoteMonitor   │  │ countCreepsByRole()  │
│ (Game.flags)    │  │  (Game.creeps 全局)  │
└─────────────────┘  └──────────┬───────────┘
                                │
         ┌──────────────────────┼──────────────────────────────┐
         ▼                      ▼                              ▼
┌──────────────┐  ┌────────────────────┐  ┌──────────────────────┐
│ RoomManager  │  │ RemoteManager      │  │ RoadPlanner          │
│ (cache+counts)│  │ (remoteRooms+cache)│  │ (cache + PathFinder) │
│ + Notifier   │  │                    │  │ CostMatrix 规划道路  │
└──────────────┘  └────────────────────┘  └──────────────────────┘
                                                   │
         ┌─────────────────────────────────────────┘
         ▼
┌──────────────┐  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
│ TowerManager │  │ LinkManager  │  │ MarketManager│  │ LabManager   │
│ (cache)      │  │ (cache.links)│  │ (cache+G.market)│  │ (cache+labs)│
└──────────────┘  └──────────────┘  └──────────────┘  └──────────────┘
                           │
                           ▼
┌──────────────────────────────────────────────────────────────────┐
│ _runAllCreeps()                                                  │
│   Game.creeps → modules[role].run(creep)                         │
│     ├── Emergency (死锁检测: RoomCache + 无采集者判定)           │
│     ├── Harvester (harvester + staticHarvester)                  │
│     ├── Upgrader  (upgrader + 强化标记 _boostLabId 响应)         │
│     ├── Builder   (builder)                                      │
│     ├── Repairer  (repairer)                                     │
│     ├── Hauler    (hauler + 化合物补给)                          │
│     ├── WallRepairer (wallRepairer)                              │
│     ├── Claimer   (claimer — 跨房间续约)                         │
│     └── RemoteHauler (remoteHauler — 跨房间搬能)                 │
│   全部通过 RoomCache.get() 读取数据，避免重复 find()             │
└──────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌──────────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│ SpawnManager.run()   │  │ StatsTracker    │  │ Visuals.run()   │
│ (counts + cache)     │  │ .record(cache)  │  │ (cache + stats) │
│ 优先级队列 → 预生成   │  │ Memory.stats    │  │ RoomVisual 绘制 │
│ BodyBuilder 动态缩放 │  │ energyRate/ETA  │  │ VisConfig 控制  │
└──────────────────────┘  └─────────────────┘  └─────────────────┘
```
